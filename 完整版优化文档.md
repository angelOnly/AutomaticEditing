# 完整版去广告 v6 优化方案

> 文档日期：2026-07-15
> 适用模式：full_concat（完整版）
> 目标栏目样本：《凤凰聚焦》
> 本文范围：给出根因、代码级改造方案、测试、灰度、回滚和验收标准。本文只做规划，不修改业务代码。
> 证据来源：最新任务归档 readme/完整版/merged_archive_multi_9_20260625_215434__完整版_07122e41ea2add95_with_common.zip、最新任务日志 job_20260715_021750_a98a24.log、四张问题截图以及当前代码。

## 一、结论先行

这次问题不是“广告关键词还不够多”，也不是 ffmpeg 裁切或拼接出了错，而是三个主问题互相放大：

1. ASR 的 chunk 所有权在归一化时被丢失。10 秒画面段最终吃到了前后共约 32 秒的完整 ASR 文本，后一个 chunk 中的“凤凰聚焦、华润、斯塔默辞职”被提前注入到通用凤凰包装画面。
2. 视觉模型已经正确识别出“通用台标包装、声画不一致、传播矩阵宣传”，但 ad_detection 分类器没有使用 visual_summary，也无法区分“栏目归属”和“栏目名只是出现在菜单里”。
3. v5 虽然新增了 frame_shots 拆分，但代码对时长小于等于 12 秒的 chunk 直接跳过拆分。生产配置恰好是 10 秒，因此这条新逻辑在真实任务中完全没有运行。

随后又有三个放大器：

- 规则层一旦把片段判为 target_intro_outro/keep，LLM 没有复审机会。
- bridge 可以从一个错误 keep 沿连续 undecided 段单侧生长，现有 120 秒配置实际只校验相邻 gap，没有限制整条链的总时长。
- build_plan 可把间隔不超过 1.5 秒的两个 keep 直接合并，即使间隔里存在明确 drop，也可能把短广告重新包回 clip。

因此，正确方向必须是一次“证据契约升级”：

- ASR 从“时间相交”改为“owner chunk 唯一归属”；
- 视觉从“有没有出现目标栏目文字”改为“该画面真正归属于哪个节目、目标名只是归属还是提及”；
- 分类从单一 detected_target 布尔值改为 HARD_DROP、STRONG_KEEP、BOUNDARY_CANDIDATE、AMBIGUOUS 四态；
- 10 秒混合段真正执行逐帧拆分，并禁止把父段 speech 复制给所有视觉子段；
- 计划生成使用区间减法，明确 drop 永远优先；
- 新增边界细化和语义 QC，不再只检查音视频轨和时长。

建议新版本统一命名为：

    ad_detection_v6_temporal_visual_ownership

## 二、目标、非目标与验收边界

### 2.1 本次必须解决

1. 片头不得混入凤凰通用台标、频道包装、华润赞助卡或其他栏目片段。
2. 片尾只保留《凤凰聚焦》自身的正片收尾和可确认属于它的职员表；版权页、其他节目播报、凤凰传播矩阵宣传、频道 App 宣传必须删除。
3. “凤凰聚焦”出现在手机、平板、节目列表、社交媒体卡片或赞助口播中，只能算提及，不能据此判为目标栏目。
4. 保留目标栏目自身真正的片头、片尾，不得因为加强去广告而删除节目首句、主持人道别或目标节目职员表。
5. 每个保留/删除结论都能在 ad_detection 产物中追溯到具体的 ASR 所有权、视觉角色、节目身份、规则路径和复审结果。
6. 新算法和旧 common pool 必须隔离，不能因缓存复用继续使用旧 ASR/视觉结果。

### 2.2 本次不做

- 不做新闻高光精选，不改变“正片尽量完整”的产品目标。
- 不通过关闭 ASR overlap 规避问题。overlap 对识别句首句尾仍有价值，问题是归属错误，不是 overlap 本身。
- 不依赖无限追加栏目名、广告词和品牌词解决结构性问题。
- 不直接把 chunk_seconds 从 10 秒继续降到 1 秒。这样成本会急剧上升，仍不能解决“栏目名提及等于归属”的语义错误。
- 不在旧 common 目录内覆盖产物；新契约生成新 key，旧产物只保留作审计和紧急回滚。

### 2.3 最终验收标准

| 类别 | 必须满足 |
|---|---|
| 本次四张截图 | 对应的通用包装、华润卡、传播矩阵和 App/频道宣传全部从新成片消失 |
| 片头 | 输出不再从 source 2 的 50.4–59.6 秒通用凤凰包装开始；目标新闻首句不丢 |
| 片尾 | source 7 的 270–280 秒版权/其他节目播报、280–290 秒华润卡、290–300.032 秒传播矩阵及尾碎片不得进入成片 |
| 目标收尾 | source 7 的 250–260 秒目标主持人口播保留；260–270 秒仅在确认是同一目标节目职员表时保留 |
| ASR | 任一新 ASR segment 只有一个 owner_chunk_id；50–60 秒不得再聚合 39–51 与 59–71 秒窗口全文 |
| 拆分 | 真实 10 秒路径 split_parent_count 大于 0；不能再出现“v5 名义上有 frame split、生产实际 0 次” |
| 计划 | hard_drop_overlap_seconds 等于 0；任何输出 clip 与明确广告/宣传区间不得相交 |
| QC | 高风险语义残留必须阻断出片或明确标 action_required，不能仍返回 status=ok |
| 回归 | 单元、集成、真实归档回放全部通过；旧有目标正片保留用例不退化 |

## 三、最新任务证据与问题区间

### 3.1 本次只采用的最新运行

- 最新 Web 任务：job_20260715_021750_a98a24
- 最新广告检测产物：edit/ad_detection/v4/ad_detection.json
- 产物外部版本：ad_detection/v4
- 产物内部算法版本：ad_detection_v5_frame_junk_split
- 目标栏目：凤凰聚焦，来源 column_bug
- 输入规模：9 个 source，每个约 31 个 10 秒 chunk，共 279 个 segment
- frame 子段数量：0
- 当前测试基线：pytest -q tests/test_ad_detection.py，29 passed

日志归档中混有公共分析过程和以前版本的产物目录。分析时以具体任务日志 job_20260715_021750_a98a24.log、manifest 当前版本和 v4 产物为准，不把 v1–v3 的旧判断混进结论。

四张截图与产物的对应关系：

| 截图文件 | 截图时间 | 内容 |
|---|---:|---|
| 31dfe6c729acc37741dcedc7fa00f3ee.png | 输出 0:09 | 华润“有华润多美好”卡出现在错误片头 clip 尾部 |
| 4d91096c72c017b4397fc3b51130430d.png | 输出 26:28 | 平板、手机和“拉近全球华人距离”的传播矩阵宣传 |
| f85d0999b4c320e58313ca54f5dd67e6.png | 输出 26:31 | “凤凰资讯 News that matters”频道/App 宣传 |
| 3040a8766a3f7c153cd29f5318f35c01.png | 输出 26:36 | 凤凰卫视、凤凰资讯、香港 V 的 App 图标尾卡 |

### 3.2 片头错误

最新 cut plan 的第一个 clip 是：

| 输出时间 | 原素材 | 原素材时间 | 当前判断 |
|---|---|---|---|
| 0–9.2 秒 | source 2 | 50.4–59.6 秒 | target_intro_outro / keep |

但 source 2 的 50–60 秒视觉结论明确是：

- 整段为凤凰卫视通用片头包装；
- 第一帧是金色凤凰卫视台标，第二帧是黄白灰几何流线；
- 没有新闻人物、新闻现场或目标栏目角标；
- ASR 提到的内容均未在画面中呈现；
- 声画不一致。

当前分类器仍把它保留，是因为这个 chunk 的 speech 被相邻窗口污染为：

    您正在收看的是凤凰卫视。凤凰聚焦由华润集团赞助播出……
    内容提及斯塔默辞职，英国政坛处于动荡状态。

这段文本同时触发 target_in_speech 和 min_news_chars，最终把通用包装提升成 target_intro_outro。

### 3.3 片尾错误

source 7 的尾部真实结构如下：

| 原素材时间 | 视觉事实 | 当前结果 | 正确结果 |
|---|---|---|---|
| 250–260 秒 | 《凤凰聚焦》主持人口播，持续目标角标 | keep | keep |
| 260–270 秒 | 同一演播室片尾、嘉宾声明和职员表 | keep | 作为目标片尾候选，经连续性确认后 keep |
| 270–280 秒 | 凤凰版权声明、IFENG.COM、无目标角标，同时播报后续《凤凰全球连线》 | target_intro_outro / keep | drop |
| 280–290 秒 | 华润“有华润多美好”赞助卡 | sponsor / drop | drop，当前这一段判对 |
| 290–300 秒 | 凤凰国际传播矩阵、平板和手机界面；“鳳凰聚焦”只是多个 UI 入口之一 | target_intro_outro / keep | drop |
| 300–300.032 秒 | 无有效画面、仅继承传播矩阵语音的 0.032 秒碎片 | bridge / keep | drop 或并入上一 hard drop |

最新 cut plan 将 290.4–300.032 秒作为最后一个 clip 接回输出，目标起点 1586.96 秒。用户截图中输出 26:28–26:36 对应原素材约 291.44–299.44 秒，正是传播矩阵宣传。

### 3.4 渲染和现有 QC 没有犯错

- cut plan 已经明确包含上述错误区间；
- 计划期望时长 1596.592 秒；
- 实际渲染时长 1596.473 秒；
- 差值仅 0.119 秒；
- final_quality_check.json 检查到音轨、视频轨、时长均正常，因此返回 status=ok。

这证明 plan/render 严格执行了错误判断。根因在分析、分类和语义 QC，不在 ffmpeg。

## 四、根因链详解

### 4.1 根因一：ASR 所有权被丢失

config.toml 当前为 10 秒 chunk、ASR 前后各 1 秒 overlap。pipeline.py 的 _asr_windows_from_chunks 已经为每个 ASR window 写入 chunk_id，step_asr 生成原始 segment 时也写入 asr_segment_id 和 chunk_id。

问题发生在 _normalize_asr_segments。它重新构造对象时只保留 id、start、end、speaker 和 text，丢掉了 chunk_id、asr_segment_id、source_id 和 window 信息。

下游三个位置只能按时间相交重新找 ASR：

- step_asr_digest；
- step_vision；
- step_timeline。

_segments_in_range 当前条件为：

    e >= start and s <= end

这既是闭区间判断，又会把“只相交 1 秒”的整个 12 秒 ASR window 全文纳入。以画面 chunk 50–60 秒为例：

| ASR window | 与 50–60 的实际重叠 | 当前行为 |
|---|---:|---|
| 39–51 | 1 秒 | 整个 12 秒文本加入 |
| 49–61 | 10 秒 | 整个 12 秒文本加入 |
| 59–71 | 1 秒 | 整个 12 秒文本加入 |

最终 10 秒画面获得约 32 秒语义范围，后续 sponsor 和新闻被提前注入。ASR digest 又会把这些内容压缩成一段看似连贯的摘要，使污染更难被发现。

### 4.2 根因二：视觉模型看对了，分类器没有使用

timeline 和 timeline_digest 已经携带：

- visual_summary / visual；
- frame_shots；
- footage_types；
- screen_text；
- column_bug。

但 ad_detection.py 的 _classify_segment 只把 speech、screen_text、scene、footage_types 拼接成 text，没有传入 visual_summary。于是下面这些正确视觉结论被完全丢弃：

- “通用片头包装”；
- “声画不一致”；
- “凤凰传播矩阵宣传”；
- “目标栏目名称只是手机/平板界面中的一个栏目入口”。

当前 detected_target 逻辑又把以下证据混成一个布尔值：

    target_by_bug
    or target_in_visual
    or target_in_speech

因此“目标栏目属于这段画面”和“目标栏目名字在画面/语音里被提到”没有区别。

### 4.3 根因三：v5 拆分在生产 10 秒路径中被禁用

_expand_visual_subsegments 的早退条件包含：

    end - start <= 12

而 full_concat 强制 chunk_seconds=10。因此：

- 单测用 60 秒模拟 chunk，拆分测试通过；
- 生产全部是 10 秒 chunk，全部直接早退；
- 最新任务仍为 9 × 31 = 279 个 segment；
- segment_id 中没有任何 _frame_ 子段。

这不是抽帧模型没识别到，也不是 _should_split_visual_chunk 没命中，而是代码在调用判断前就把生产路径排除了。

### 4.4 根因四：父段 speech 被复制到每个视觉子段

即使删除 12 秒早退，当前 sub = dict(chunk) 仍会把父 chunk 的完整 speech 复制给每一个 frame 子段。

例如 60–70 秒第一帧是华润卡，第二帧已进入新闻。若拆成两个视觉子段，两个子段都会拿到“凤凰聚焦、华润、斯塔默辞职”的同一份父 speech，华润子段仍可能被新闻字符数救回。

因此“开启拆分”不是完整修复；必须同时改成：

- 子段使用本帧视觉字段；
- 只有有可靠时间归属的 ASR 单元才进入子段 speech；
- 无句级时间时，父 speech 只能存为 speech_context，不能参与子段硬规则。

### 4.5 根因五：规则错误 keep 无法被 LLM 纠正

detect 只把 keep_state=undecided 的片段放入 ambiguous，再送 LLM。错误的 target_intro_outro 是规则直接 keep，因此完全绕过 LLM。

现有 ad_detection 提示词还写着：

- screen_text 出现目标栏目基本就是 target；
- 拿不准优先 target。

这会进一步把传播矩阵菜单中的“鳳凰聚焦”当成栏目归属。

### 4.6 放大器：bridge 没有限制整条链总时长

_resolve_keep 当前从任一 keep 向左右迭代传播。bridge_max_gap_seconds=120 只用于比较相邻两个 segment 的物理间隔。

连续 10 秒 chunks 的相邻 gap 永远为 0，所以一条连续 undecided 链可以从一个错误 keep 一直生长，并不受 120 秒“总跨度”限制。尾部 300–300.032 秒碎片就是被错误 keep 的 290–300 秒传播矩阵带成 bridge。

### 4.7 放大器：build_plan 可能把短 drop 合并回来

build_plan 先收集 keep，再在 keep 间距小于等于 merge_gap_seconds=1.5 时合并。这个合并没有检查 gap 中是否存在明确 drop。

_apply_safety_margin 只处理 drop 端点与 clip 起止点相接的情况，不能修复“明确 drop 位于被合并区间内部”的问题。

### 4.8 放大器：缓存 key 没有分析契约

common pool key 当前由 web_app.py 和 run_multisource_pipeline.py 各算一次，包含素材、画幅、chunk、frame 和 mode，但没有：

- ASR 归属算法版本；
- vision schema / prompt 版本；
- timeline digest 契约；
- source summary / aggregate 的分析 profile。

而 _common_analysis_ready 只检查文件是否存在和步骤是否 ready。即使代码已升级，只要 10/5 参数没变，旧 common pool 仍可能被静默复用。

另外，PipelineRunner.run 在调用 handler 前执行 _mark_running，覆盖旧 step 的 success/input_hash/output；随后 handler 内 _can_reuse 读取到 running，无法正常命中旧步骤缓存。common 层和 step 层的缓存语义不一致，也必须在本次版本化改造时修正并补测试。

## 五、改造后的证据原则

### 5.1 必须区分“节目归属”和“节目提及”

强归属证据：

- 当前帧或连续帧的栏目角标明确等于目标栏目；
- 目标栏目名作为独占、主导的片头大标题，且 content_role 是 program_title；
- 当前画面被结构化识别为 program_identity=目标栏目；
- 同一演播室的职员表紧贴强目标正片，中间没有任何 hard drop。

弱提及证据，绝不能单独 keep：

- speech 中说到“凤凰聚焦”；
- 赞助口播里说“凤凰聚焦由华润集团赞助播出”；
- 手机、平板、网页、节目列表、菜单、传播矩阵里出现“鳳凰聚焦”；
- 其他节目在播报“稍后播出凤凰聚焦”；
- 排期表推断。

强删除证据：

- sponsor_card；
- commercial_ad；
- other_program_promo；
- channel_promo；
- platform_promo；
- multi_program_promo；
- station_id；
- copyright_slate；
- 非目标栏目本地角标或非目标 program_identity。

优先级必须为：

    hard visual drop
    > 非目标节目强归属
    > 目标节目强归属
    > 片头/片尾边界候选
    > owned speech 和新闻量
    > 目标名提及、排期和 legacy 上下文

### 5.2 《凤凰聚焦》自身片头片尾的严格定义

允许保留：

- 独占主标题明确为“凤凰聚焦”的节目片头；
- 目标角标持续出现的主持人口播、正片；
- 同一演播室、物理连续、紧贴强目标段的节目职员表；
- 由边界细化确认与目标正片连续的目标节目片尾动画。

必须删除：

- 只有凤凰台标的金色/抽象包装；
- 华润赞助卡；
- 凤凰版权页；
- 凤凰资讯、凤凰卫视、IFENG.COM 的频道宣传；
- 国际传播矩阵、App、手机、平板宣传；
- 菜单里包含“凤凰聚焦”但画面主语是整个平台；
- 其他栏目预告或接下来节目播报。

## 六、新的数据契约

### 6.1 统一版本常量

新增 newsclip_agent/analysis_contracts.py，集中维护：

    ASR_WINDOW_CONTRACT_VERSION = "asr_window_v2_core_owned"
    ASR_SEGMENT_CONTRACT_VERSION = "asr_segments_v2_core_owned"
    ASR_ATTRIBUTION_POLICY_VERSION = "owner_chunk_then_midpoint_legacy_v2"
    ASR_DIGEST_CONTRACT_VERSION = "asr_digest_v2_owned_text"
    VISION_SCHEMA_VERSION = "vision_chunk_v4_content_ownership"
    VISION_ASR_INPUT_CONTRACT_VERSION = "vision_asr_input_v2_owned_text"
    TIMELINE_CONTRACT_VERSION = "timeline_v2_owned_asr_visual_ownership"
    TIMELINE_DIGEST_CONTRACT_VERSION = "timeline_digest_v3_owned_evidence"
    COMMON_ANALYSIS_CONTRACT_VERSION = "common_analysis_v2_owned_evidence"
    AD_DETECTION_CONTRACT_VERSION = "ad_detection_v6_temporal_visual_ownership"
    BOUNDARY_REFINE_CONTRACT_VERSION = "ad_boundary_refine_v1"
    PLAN_QC_CONTRACT_VERSION = "full_concat_plan_qc_v1"
    OUTPUT_QC_CONTRACT_VERSION = "full_concat_output_qc_v1"

这些版本必须同时写入：

- JSON 产物 version / contract_version；
- 对应步骤 input_hash；
- common analysis profile；
- common source key；
- common_state.json 和 manifest；
- 日志和报告。

只改常量不进入 hash 等于没有版本化。

### 6.2 ASR window 和 segment schema

ASR window 同时保存核心区间与实际解码区间：

    {
      "window_id": "chunk_0006__asr",
      "owner_chunk_id": "chunk_0006",
      "source_id": "source_002",
      "core_start": 50.0,
      "core_end": 60.0,
      "decode_start": 49.0,
      "decode_end": 61.0,
      "overlap_before_seconds": 1.0,
      "overlap_after_seconds": 1.0
    }

ASR segment 增加：

    {
      "owner_chunk_id": "chunk_0006",
      "window_id": "chunk_0006__asr",
      "source_id": "source_002",
      "start": 49.0,
      "end": 61.0,
      "core_start": 50.0,
      "core_end": 60.0,
      "decode_start": 49.0,
      "decode_end": 61.0,
      "timing_quality": "window_coarse",
      "text_scope": "decode_window",
      "strict_evidence_eligible": false,
      "text": "..."
    }

当前 FunASR 常把整个 window 返回成一个粗 segment，因此 owner_chunk_id 可用于唯一归属，但 timing_quality=window_coarse 的文本不能单独证明“目标栏目在这一帧出现”。

### 6.3 vision 公共 schema

vision 公共池不能写死“凤凰聚焦”这个目标，否则不能复用；它应输出与目标无关的节目身份：

    {
      "visual_summary": "...",
      "asr_visual_relation": "match|mismatch|unknown",
      "content_role": "news_content|program_title|program_credits|sponsor_card|commercial_ad|other_program_promo|channel_promo|platform_promo|multi_program_promo|station_id|copyright_slate|transition|unknown",
      "program_identity": "",
      "program_identity_confidence": 0.0,
      "column_bug_confidence": 0.0,
      "program_mentions": [
        {"name": "凤凰聚焦", "role": "menu_item"}
      ],
      "frames": [
        {
          "t": 290.0,
          "shot_type": "片头包装",
          "visual": "平板和手机显示凤凰传播矩阵",
          "screen_text": ["鳳凰秀", "鳳凰聚焦"],
          "column_bug": "",
          "column_bug_confidence": 0.0,
          "content_role": "platform_promo",
          "program_identity": "",
          "program_identity_confidence": 0.0,
          "program_mentions": [
            {"name": "凤凰聚焦", "role": "menu_item"}
          ]
        }
      ]
    }

ad_detection 在知道目标栏目后，再从这些通用字段派生：

    target_visual_evidence =
      strong_ownership | boundary_candidate | mention_only | none | conflict

### 6.4 检测证据对象

建议新增内部 Evidence 结构或等价字典，避免 _classify_segment 参数继续膨胀：

    {
      "source_id": "...",
      "start": 290.0,
      "end": 300.0,
      "owned_speech": "...",
      "speech_context": "...",
      "speech_scope": "owned|parent_context|legacy_context",
      "visual_summary": "...",
      "frame_shots": [...],
      "content_role": "platform_promo",
      "program_identity": "",
      "program_mentions": [...],
      "local_column_bug": "",
      "target_visual_evidence": "mention_only",
      "asr_visual_relation": "match",
      "evidence_quality": "v2_structured"
    }

硬规则只读 owned_speech；speech_context 只给 LLM 和人工审计，不参与 target 命中或 residual news 计算。

## 七、逐文件、逐函数修改方案

### 7.1 新增 newsclip_agent/analysis_contracts.py

修改内容：

- 放置第六章的契约常量和 ASR 时间 epsilon；
- 提供 build_analysis_profile(config, options)；
- profile 只纳入会改变语义的配置，不纳入 API key、GPU device、worker 数等运行参数；
- 提供 profile_hash。

为什么：

- web、runner、pipeline 和 detector 必须引用同一个版本源；
- 避免 Web 和命令行各写一套字符串，造成同素材生成不同 common key。

### 7.2 pipeline.py：ASR 唯一归属

#### A. _asr_windows_from_chunks（当前约 737–780 行）

改法：

1. 保留现有 overlap；
2. 新增 core_start/core_end 和 decode_start/decode_end；
3. owner_chunk_id 明确等于 chunk_id；
4. 多源始终带 source_id；
5. decode_end 钳制到源时长；
6. 兼容期可保留旧 start/end 字段，但文档明确它们是 decode range。

伪代码：

    for chunk in chunks:
        core_start, core_end = chunk_range(chunk)
        decode_start = max(0.0, core_start - overlap)
        decode_end = min(source_duration, core_end + overlap)
        windows.append({
            "window_id": f"{chunk_id}__asr",
            "owner_chunk_id": chunk_id,
            "source_id": source_id,
            "core_start": core_start,
            "core_end": core_end,
            "decode_start": decode_start,
            "decode_end": decode_end,
            "start": decode_start,
            "end": decode_end,
        })

#### B. step_asr（当前约 1987–2105 行）

改法：

- input_hash 的 segmented_asr_v1 改为 ASR_SEGMENT_CONTRACT_VERSION；
- 原始 segment 写入 owner_chunk_id、source_id、window/core/decode range；
- 有可靠模型时间戳时标 model_timestamp，否则标 window_coarse；
- full_text 只拼 owned segment；带 overlap 的原始拼接另存 raw_decode_full_text 作诊断；
- 若某个 window 有文本但没有 segments，不再生成全局 0–0 segment，而是在该 window 内创建 window_coarse segment。

为什么：

- owner 在生成时最准确，不能事后用相交范围猜；
- 保留 overlap 的识别收益，同时禁止 overlap 文本成为相邻画面的硬证据。

#### C. _normalize_asr_segments（当前约 11555–11573 行）

改法：

- 用白名单保留 owner_chunk_id、chunk_id、window_id、source_id、asr_segment_id、raw_text、removed_tokens、core/decode range、timing_quality、text_scope 和 strict_evidence_eligible；
- 只清洗 text 和规范 start/end，不再丢 provenance。

#### D. 新增 _asr_segments_for_chunk

正常路径：

    if any segment has owner_chunk_id:
        return segments where
          owner_chunk_id == chunk_id
          and source_id compatible
          and not context_only

旧产物兼容：

- 使用 segment midpoint 唯一归属到半开区间 [start, end)；
- 每个 legacy segment 最多归属一个 chunk；
- 标记 attribution_policy=legacy_midpoint_recovered；
- strict_evidence_eligible=false。

不要全局直接删除 _segments_in_range，因为其他业务可能仍需要范围查询。若保留该函数，至少改成正重叠：

    overlap = max(0, min(seg_end, end) - max(seg_start, start))
    overlap > epsilon

端点恰好相等不算相交。

#### E. 替换三个消费点

- step_asr_digest 当前约 2167 行；
- step_vision 当前约 2296 行；
- step_timeline 当前约 2420 行。

三处全部改用 _asr_segments_for_chunk。只改其中一处会让污染从另一路重新进入 timeline。

#### F. step_asr_digest

每个 chunk 输出分离：

    {
      "chunk_id": "...",
      "owned_text": "规则和审计使用的原始归属文本",
      "speech": "LLM 压缩摘要",
      "segment_ids": [...],
      "attribution": {
        "policy": "...",
        "timing_quality": [...],
        "has_coarse_timing": true
      }
    }

_residual_news_len 只能对 owned_text 计算，不再对 speech + visual_text 的混合文本计算。

#### G. step_vision 的局部重跑保护

vision 输入 hash 增加 VISION_ASR_INPUT_CONTRACT_VERSION。若 ASR contract 或 digest hash 改变，而用户使用 --chunk 或 --failed-only：

- 推荐自动升级为全量 vision 重跑；
- 至少不得复制上一 vision 版本中基于旧 ASR 的其他 chunk。

否则同一个 vision/vN 会混有新旧 ASR 上下文。

### 7.3 prompt_texts/vision_chunk.txt

修改输出 schema：

- chunk 级增加 asr_visual_relation、content_role、program_identity、program_identity_confidence、column_bug_confidence、program_mentions；
- frame 级增加 screen_text、column_bug、column_bug_confidence、content_role、program_identity、program_identity_confidence、program_mentions；
- 明确 menu_item/list_item 不是节目归属；
- 明确通用凤凰台标、版权、频道宣传不等于任何具体栏目；
- ASR 只能辅助判断声画关系，不能填充画面中不存在的 program_identity。

为什么：

- 仅靠自由文本 visual_summary 虽能看懂，但规则难以稳定消费；
- 公共 schema 仍与目标栏目无关，可以继续服务其他模式。

### 7.4 pipeline.py：vision 物化、timeline 和 digest 透传

#### A. _materialize_vision_chunk_result（当前约 5000–5052 行）

当前每帧只保留 t、shot_type、visual。必须增加：

- frame.screen_text；
- frame.column_bug；
- frame.content_role；
- frame.program_identity；
- frame.program_mentions。

chunk 级增加：

- content_role；
- program_identity；
- program_identity_confidence；
- column_bug_confidence；
- program_mentions；
- asr_visual_relation。

所有枚举需代码校验；非法值归为 unknown，列表字段非 list 时归空列表。

为什么：

- 只改 prompt 不改物化函数，新字段仍会被代码丢掉；
- frame 级角色是 10 秒混合段拆分的基础。

#### B. step_timeline（当前约 2414–2452 行）

继续保留 visual_summary、frame_shots、footage_types，并新增：

- asr_owned_text；
- asr_attribution；
- content_role；
- program_identity；
- program_mentions；
- asr_visual_relation。

#### C. step_timeline_digest（当前约 2472–2496 行）

当前显式构造字段，需把上述字段加入。不能只依赖 source_aggregate 的字典拷贝，因为字段若在 timeline_digest 这里丢失，后面无法恢复。

建议保留轻量 speech_segments：

    [{"start": ..., "end": ..., "text": ..., "owner_chunk_id": ...}]

若体积受限，至少保留 owned_text、speech_scope 和 attribution。

### 7.5 pipeline.py：source_analysis/source_aggregate 契约

#### A. step_source_analysis（当前约 846 行）

analysis_profile 增加：

- COMMON_ANALYSIS_CONTRACT_VERSION；
- ASR window/segment/attribution 版本；
- ASR digest 版本；
- vision schema/prompt/model；
- timeline/digest 版本；
- chunk_seconds、frame_interval、vision_max_frames_per_chunk；
- 会改变语义的 FunASR 配置。

profile_hash 写入：

- source_analysis input_hash；
- source_status.json；
- source_summary.json；
- source_aggregate；
- manifest/common_state。

#### B. 单 source summary 复用（当前约 955–979 行）

现状只看 summary 文件存在且 status=success。增加：

    source_status.analysis_profile_hash == expected
    and source_summary.analysis_profile_hash == expected

不一致时必须重跑，不能直接返回 reused=true。

#### C. raw ASR index

新 ASR 已有 owner_chunk_id，source aggregate 应直接映射成 global owner：

    global_owner_chunk_id = f"{source_id}_{owner_chunk_id}"

primary_chunk_id 和 source_chunk_ids 都只写这个 owner。只有 legacy 数据才走时间推断。

版本同步升级：

- source_raw_asr_index_v2_core_owned；
- source_aggregate_local_time_v2_owned_evidence；
- multi_source_local_timeline_digest_v2_owned_evidence。

### 7.6 ad_detection.py：先解析目标，再拆视觉段

当前 detect 先 expand，再 resolve_target_column。建议调整为：

    target = resolve_target_column(original_parent_chunks)
    expanded = expand_visual_subsegments(original_parent_chunks)
    classify(expanded, target)

为什么：

- 拆分可能清空或重复父 column_bug；
- 目标栏目应从原始、持续、高置信角标确定，不应由拆分子段数量改变投票结果。

resolve_target_column 同时优化：

- manual override 始终最高；
- column_bug 按有效持续时长和 confidence 加权，不再每个 chunk 一票；
- sponsor/platform/channel promo 中的栏目文字不参与目标投票；
- schedule 只做兜底。

### 7.7 ad_detection.py：修复 10 秒视觉拆分

_expand_visual_subsegments 修改：

1. 删除 end-start<=12 的早退；
2. len(shots)>=2 且相邻帧的 content_role、program_identity、column_bug 或硬杂质 marker 发生变化时拆分；
3. 不再 sub=dict(chunk)，显式构造子段；
4. 子段 column_bug 只来自本帧结构化 column_bug，不能复制父 bug；
5. 子段 speech 只接受时间真正属于子区间的 ASR；无法精确切分时留空；
6. 父 owned_speech 只放 speech_context；
7. 每个子段记录 parent_segment_id、split_reason、boundary_confidence 和 speech_scope；
8. 断言所有子段无重叠、无空洞、并完整覆盖父区间 [start,end)。

伪代码：

    def expand(parent):
        shots = normalized_frame_shots(parent)
        if len(shots) < 2 or not visual_state_changed(shots):
            return [materialize_parent(parent)]

        children = []
        for index, shot in enumerate(shots):
            start = parent.start if index == 0 else shot.t
            end = parent.end if index + 1 == len(shots) else shots[index + 1].t
            children.append({
                **explicit_source_and_time_fields(parent, start, end),
                "visual_summary": shot.visual,
                "screen_text": shot.screen_text,
                "column_bug": shot.column_bug,
                "content_role": shot.content_role,
                "program_identity": shot.program_identity,
                "program_mentions": shot.program_mentions,
                "speech": owned_child_speech(start, end),
                "speech_context": parent.owned_speech,
                "speech_scope": "owned" if exact else "parent_context",
            })
        assert_interval_partition(children, parent)
        return children

source 2 的 60–70 秒应拆为：

- 60–约 65 秒：sponsor_card，drop；
- 约 65–70 秒：news_content，作为目标/bridge 候选。

不能再因父 speech 同时包含 sponsor 和新闻而整段保留或整段删除。

### 7.8 ad_detection.py：分类器改成两阶段状态机

把 _classify_segment 拆成：

- _extract_evidence；
- _classify_local；
- _resolve_context_candidates。

本地分类伪代码：

    def classify_local(ev):
        if ev.content_role in HARD_DROP_ROLES:
            return HARD_DROP, role_to_label(ev.content_role)

        if ev.local_other_bug or ev.program_identity_is_other:
            return HARD_DROP, "other_column"

        if ev.local_target_bug and not ev.has_hard_visual_conflict:
            return STRONG_KEEP, "target"

        if ev.program_identity_is_target and ev.content_role == "news_content":
            return STRONG_KEEP, "target"

        if (
            ev.program_identity_is_target
            and ev.content_role in {"program_title", "program_credits"}
        ):
            return BOUNDARY_CANDIDATE, "target_intro_outro"

        if ev.is_generic_packaging:
            return HARD_DROP, "packaging_other"

        if ev.has_owned_news_speech and ev.content_role == "news_content":
            return AMBIGUOUS, "unknown"

        return AMBIGUOUS, "unknown"

关键变化：

- HARD_DROP_ROLES 命中时，不再让 target speech、news_substantial 或目标文字救回；
- min_news_chars 只在非 hard-drop 的 news_content/unknown 角色中使用；
- residual 只计算 owned_speech，不把 screen_text、visual_summary、节目菜单文字算成“新闻字符”；
- target_in_speech 只产生 mention_only，不直接产生 detected_target；
- menu/list/social UI 命中目标名只产生 mention_only；
- 通用包装没有 program_identity=target 时一律 packaging_other；
- 规则结果记录 decision_state、decision_path、evidence、risk_flags 和 confidence。

### 7.9 ad_detection.py：目标片头片尾上下文确认

BOUNDARY_CANDIDATE 只有满足以下条件才变为 keep：

- 同一 source；
- 与 STRONG_KEEP 目标 anchor 物理连续，建议 gap 小于等于 0.5 秒；
- 从 anchor 到候选链的总跨度不超过 10–15 秒；
- 中间无 HARD_DROP；
- 视觉是同一节目片头、同一演播室职员表或独占目标标题；
- 不是 station/channel/platform/sponsor/copyright 角色。

source 7 的预期：

- 250–260 STRONG_KEEP；
- 260–270 program_credits + 同演播室 + 紧邻目标，BOUNDARY_CANDIDATE 最终 keep；
- 270–280 copyright_slate，HARD_DROP，立刻阻断；
- 280 秒后的所有宣传不能借前面的目标 anchor 生长。

### 7.10 ad_detection.py：bridge 改为有限候选链

新增配置：

    bridge_max_total_seconds = 20.0
    bridge_max_gap_seconds = 0.5
    edge_extension_max_seconds = 8.0

算法：

- interior bridge：一整条 ambiguous chain 两侧都必须是 STRONG_KEEP，且总时长不超过 bridge_max_total_seconds；
- edge extension：只允许素材开头或结尾单侧扩展，且总时长不超过 edge_extension_max_seconds，并必须有 news_content、同演播室或 owned news speech；
- LLM 低置信 target 不能作为 bridge anchor；
- HARD_DROP、source boundary、真实时间断裂立即终止；
- subsecond 无画面尾碎片默认继承相邻 HARD_DROP，不允许因“有父语音”成为 bridge。

这与现有“从任一 keep 单侧无限迭代生长”不同。

### 7.11 ad_detection.py 与 prompt_texts/ad_detection.txt：高风险 keep 复审

LLM 不只接收 label=None。新增 needs_review：

- 包装画面 + 目标 speech；
- screen_text 含目标名但 program_mentions.role 是 menu_item/list_item；
- asr_visual_relation=mismatch；
- target_intro_outro 边界候选；
- legacy_low 证据；
- 规则证据互相冲突。

payload 增加：

- visual_summary；
- frame_shots；
- content_role；
- program_identity；
- program_mentions；
- asr_visual_relation；
- owned_speech；
- speech_context 和 speech_scope；
- 左右 STRONG_KEEP/HARD_DROP anchor；
- rule evidence 和 risk flags。

提示词改为：

- 目标栏目名在菜单、社交平台、节目列表、赞助口播中仅为提及；
- audio mention 不能证明当前画面归属；
- hard visual junk 优先；
- 只有 program ownership 才能返回 target；
- 不再写“拿不准优先 target”。

返回字段：

    {
      "segment_id": "...",
      "label": "target|other|ad|promo|sponsor|station_id|unknown",
      "confidence": 0.0,
      "evidence_type": "ownership|mention|visual_role|context",
      "reason": "..."
    }

代码约束：

- LLM 不得把 HARD_DROP 翻成 keep，只能审计或补充原因；
- 没有 ownership evidence 的 target 返回只能是 AMBIGUOUS，不能成为 STRONG_KEEP 或 bridge anchor；
- LLM 失败时，包装/宣传按 hard drop；普通新闻候选进入有限 bridge，不崩溃。

### 7.12 ad_detection.py：build_plan 使用区间代数

生成计划前先做：

    hard_drops = union(all explicit drop intervals)
    raw_keeps = union(all keep intervals)
    effective_keeps = subtract(raw_keeps, hard_drops)

合并条件：

    gap <= merge_gap
    and no explicit drop overlaps the gap

最后增加强断言：

    for clip in clips:
        assert overlap(clip, any_hard_drop) == 0

若断言失败：

- full_concat_plan 标 failed/action_required；
- 不继续渲染；
- 报告 source_id、clip 区间、drop 区间和 label。

boundary_reason 不再留空，写入：

- split/refine 依据；
- 相邻 drop label；
- margin；
- confidence；
- 是否来自 legacy fallback。

safety_margin_seconds 只作为边界确认后的最终保护，不再承担广告检测职责。

### 7.13 新增 ad_boundary_refine 工作流步骤

建议放在 ad_detection 与 full_concat_plan 之间：

    ad_detection
      → ad_boundary_refine
      → full_concat_plan

建议把纯边界算法放入新增 newsclip_agent/full_concat_boundary.py，pipeline.py 只负责加载源媒体、调用、写产物和登记步骤。模块至少提供 collect_risk_windows、detect_scene_change_candidates、find_stable_transition、apply_refined_boundaries 和 assert_interval_partition，避免继续把媒体抽帧逻辑堆入 ad_detection.py。

输入：

- 初步 segments；
- frame_shots；
- 源视频路径；
- 所有 keep/drop 状态变化区间。

只处理高价值边界，不全片加密抽帧：

1. HARD_DROP → KEEP；
2. KEEP → HARD_DROP；
3. BOUNDARY_CANDIDATE 两侧；
4. 头部前 20 秒和尾部后 30 秒；
5. 5 秒两探针给出不同角色的 mixed chunk。

在粗边界两侧按 0.5–1 秒补抽帧，或先用 scene change 缩小范围。连续两帧同类才确认状态，避免单帧误判。

产物 edit/ad_boundary_refine/vN/ad_boundary_refine.json：

    {
      "version": "ad_boundary_refine_v1",
      "segments": [...],
      "boundaries": [
        {
          "source_id": "...",
          "coarse_range": [60.0, 65.0],
          "refined_at": 63.5,
          "from_state": "sponsor_card",
          "to_state": "target",
          "confidence": 0.92,
          "evidence_frames": [...]
        }
      ],
      "stats": {
        "candidate_count": 0,
        "refined_count": 0,
        "fallback_count": 0
      }
    }

为什么：

- 5 秒 frame_interval 只能把转场圈在 5 秒范围内；
- 固定 0.4 秒 margin 无法判断真实转场；
- 选择性补帧成本远低于把全片改成 1 秒抽帧。

### 7.14 新增 full_concat_plan_qc

这是渲染前语义门禁，放在 plan 与 render 之间：

    full_concat_plan
      → full_concat_plan_qc
      → full_concat_render

它读取源素材证据、边界精修结果和 cut plan，低成本检查：

- 计划输出开头 30 秒和结尾 30 秒；
- 每个 drop 后重新开始的 keep island；
- 所有 target_intro_outro；
- 所有 mention_only、visual mismatch、legacy_low keep；
- 所有小于 3 秒的碎片；
- 每个 clip 首尾 2–3 秒；
- 每个拼接点两侧。

强制阻断项：

- sponsor_card_kept；
- station/platform/channel_promo_kept；
- other_program_kept；
- target_only_mentioned_not_owned；
- generic_packaging_at_head_or_tail；
- explicit_drop_reintroduced_by_merge；
- unresolved_high_risk_boundary；
- source_time_out_of_bounds；
- clip overlap、倒序或与 hard drop 相交。

结果：

- high：阻断渲染，manifest 标 action_required；
- medium：允许配置决定阻断或告警；
- low：记录报告。

产物建议为 edit/full_concat_plan_qc/vN/full_concat_plan_qc.json，必须同时记录 source 时间和映射后的 output 时间，便于直接把输出 26:28 追到 source 7 约 291.44 秒。

### 7.15 新增 full_concat_output_qc

plan QC 仍与 detector 使用同一批源证据，不能证明最终 MP4 一定正确。因此渲染后再对实际成片做独立语义复核：

    full_concat_render
      → full_concat_output_qc

抽检范围：

- 实际成片前 15–30 秒，基础 1 fps，有疑点时加密到 2–4 fps；
- 实际成片后 15–30 秒；
- 每个真实拼接点前后 2 秒；
- plan QC 标记的所有高风险输出区间。

输出 QC 必须直接从最终 MP4 抽帧，不能只重新读取 ad_detection.json，否则同一个上游误判会同时骗过检测与 QC。建议使用独立 OUTPUT_QC_CONTRACT_VERSION 和独立的轻量角色判定，明确识别 sponsor、传播矩阵、节目菜单、通用凤凰包装和其他栏目。

判定：

- 首尾出现任一高置信非目标帧：fail；
- 中间连续两帧或累计至少 1 秒明确非目标内容：fail；
- 单帧不确定：加密抽帧复查；
- QC 服务不可用：qc_unavailable/block，不能默认通过。

产物 edit/full_concat_output_qc/vN/full_concat_output_qc.json 写入 publishable、issues、抽检帧、技术 QC 引用和语义 QC 结果。失败 MP4 不删除，标 quarantined=true 作诊断预览，但不能被 Web 当作“最新成片”或任务完成结果。

当前 final_quality_check 继续负责时长、解码和音视频轨。新版任务只有技术 QC 与 output semantic QC 都通过，才允许 publishable=true。

建议把 plan/output 两级纯检查逻辑放入新增 newsclip_agent/full_concat_qc.py。render index 同时保存最终 MP4 的 size_bytes、mtime_ns 和 sampled_sha256；output QC 的 input_hash 必须包含这个媒体指纹，避免 MP4 被替换后仍复用旧 QC。

### 7.16 workflow_registry.py

full_concat 两条 workflow 都插入：

    ad_detection
    ad_boundary_refine
    full_concat_plan
    full_concat_plan_qc
    full_concat_render
    full_concat_output_qc

依赖修改：

    "ad_boundary_refine": ["ad_detection"]
    "full_concat_plan": ["ad_boundary_refine"]
    "full_concat_plan_qc": ["full_concat_plan", "ad_boundary_refine"]
    "full_concat_render": ["full_concat_plan", "full_concat_plan_qc"]
    "full_concat_output_qc": ["full_concat_render", "full_concat_plan"]

同时更新：

- only_analysis/skip_render 的步骤跳过规则；skip_render 时 output QC 也必须跳过；
- step label；
- rerun/rerun-from 校验；
- common reusable steps不得包含目标栏目相关步骤。

Web 完成态也要同步：新版 full_concat 只有 full_concat_output_qc 成功且 publishable=true 才算完成。ad-report 返回 boundary_refinement、plan_qc、output_qc、quarantined 和 issue 的源/输出时间；latest video 默认排除 quarantined 文件，失败成片单列为诊断预览。

### 7.17 web_app.py、run_multisource_pipeline.py 与 common cache

建议新增 newsclip_agent/common_cache.py，Web 与 runner 共用：

    def build_common_analysis_profile(config, request):
        ...

    def build_common_source_key(source_fingerprints, analysis_profile):
        return stable_hash({
            "sources": source_fingerprints,
            "analysis_profile": analysis_profile,
        })[:16]

修改 web_app.py 的 _common_source_key 和 run_multisource_pipeline.py 的 _request_common_source_key，统一调用该函数。

profile 至少包含：

- COMMON_ANALYSIS_CONTRACT_VERSION；
- ASR/vision/timeline 契约；
- chunk/frame/mode/aspect；
- vision_max_frames_per_chunk；
- 会改变语义的模型和 prompt 版本。

runner 每次启动都重新计算 expected key，不能只在 chunk/frame 发生变化时重算。

_common_analysis_ready 接收 expected_profile_hash，并同时验证：

- common manifest；
- common_state.json；
- source summaries；
- 必需步骤产物。

任何 profile 不一致都返回 false。旧目录不删除，新 key 创建新 common 目录。

common source_aggregate hash 变化后，业务任务自动把以下步骤标 stale：

    ad_detection
    ad_boundary_refine
    full_concat_plan
    full_concat_plan_qc
    full_concat_render
    full_concat_output_qc

### 7.18 pipeline.py：修复步骤级缓存候选被覆盖

run 在 _mark_running 前保存：

    self._cache_candidates[step] = deepcopy(previous_step_entry)

_can_reuse 从 cache_candidates 读取旧 success/input_hash/output，命中后恢复旧记录并更新时间，而不是读取已被改为 running 的 manifest entry。

为什么：

- 否则新增的 input_hash/version 设计无法被可靠验证；
- common 外层会粗粒度复用，step 内层却永远重跑，行为相互矛盾。

### 7.19 config.toml

建议新增或调整：

    [full_concat]
    chunk_seconds = 10
    frame_interval = 5
    bridge_max_gap_seconds = 0.5
    bridge_max_total_seconds = 20.0
    edge_extension_max_seconds = 8.0

    [ad_detection]
    detector_version = "v6"
    use_structured_visual_ownership = true
    hard_drop_visual_precedence = true
    review_conflicting_keeps = true
    merge_gap_seconds = 1.5
    safety_margin_seconds = 0.4

    [ad_boundary_refine]
    enabled = true
    probe_interval_seconds = 1.0
    min_confirming_frames = 2
    max_probe_window_seconds = 6.0

    [full_concat_plan_qc]
    enabled = true
    head_seconds = 30
    tail_seconds = 30
    block_on_high_risk = true

    [full_concat_output_qc]
    enabled = true
    mode = "block"
    head_seconds = 30
    tail_seconds = 30
    join_window_seconds = 2
    publish_requires_pass = true

清理或落实当前未真正参与决策的配置：

- conservative；
- min_isolated_ad_seconds；
- extra_news_keywords；
- traditional_to_simplified。

每个配置要么接入代码并补测试，要么删除，避免运维误以为已经生效。

## 八、关键伪代码汇总

### 8.1 ASR owner 选择

    def asr_segments_for_chunk(segments, chunk):
        owned = [
            seg for seg in segments
            if seg.owner_chunk_id == chunk.chunk_id
            and same_source(seg, chunk)
            and not seg.context_only
        ]
        if owned:
            return dedupe(owned)

        # 仅兼容旧产物；一个 segment 只能归到一个 chunk
        return [
            seg for seg in segments
            if chunk.start <= midpoint(seg.start, seg.end) < chunk.end
        ]

### 8.2 视觉状态拆分

    if len(frame_shots) >= 2 and visual_state_changed(frame_shots):
        children = partition_parent_by_shots()
        for child in children:
            child.speech = exact_owned_speech(child.range)
            child.speech_context = parent.owned_speech
            child.column_bug = child.frame.column_bug
        assert partition_is_exact(children, parent)

### 8.3 四态分类

    HARD_DROP:
        强视觉杂质、其他节目归属

    STRONG_KEEP:
        当前画面有强目标节目归属

    BOUNDARY_CANDIDATE:
        目标片头、片尾、职员表，需上下文确认

    AMBIGUOUS:
        普通新闻画面、弱语音提及或 legacy 证据，交 LLM/有限 bridge

### 8.4 有限 bridge

    for each ambiguous_chain:
        if hard_drop_inside(chain):
            drop(chain)
        elif strong_keep_on_both_sides(chain) and chain.duration <= 20:
            keep_as_bridge(chain)
        elif at_source_edge(chain) and chain.duration <= 8 and target_like(chain):
            keep_as_edge_extension(chain)
        else:
            drop(chain)

### 8.5 计划区间减法

    effective_keep = union(keep_intervals) - union(hard_drop_intervals)
    clips = merge_only_when_gap_has_no_drop(effective_keep)
    assert overlap(clips, hard_drop_intervals) == 0

## 九、测试与回归方案

### 9.1 新增 ASR 归属测试

新增 tests/test_asr_attribution.py：

1. 10 秒 chunk + 1 秒 overlap 同时生成 core/decode range；
2. normalize 后 owner_chunk_id/source_id/window_id 不丢；
3. 构造 39–51=A、49–61=B、59–71=C，查询 50–60 只能得到 B；
4. 恰好结束于 50 的 segment 不进入 50–60；
5. 恰好开始于 60 的 segment 不进入 50–60；
6. 相同本地秒数、不同 source_id 不串；
7. legacy midpoint 每段最多归属一个 chunk；
8. 最后一个 decode_end 不超过素材时长；
9. window_coarse 不具备 strict evidence 资格；
10. timeline、vision input 和 asr_digest 使用同一 owned_text。

### 9.2 扩展 tests/test_ad_detection.py

必须使用真实 10 秒形态，而不是只用 60 秒模拟：

1. generic Phoenix 包装 + parent speech 含“凤凰聚焦/华润/后续新闻”仍必须 drop；
2. 290–300 秒 platform_promo，menu_item 含“鳳凰聚焦”，必须 drop；
3. 10 秒两帧角色不同，必须生成 _frame_ 子段；
4. sponsor/news 混合父段拆成 sponsor drop + news keep/bridge；
5. 子段 speech 不等于父段 speech，父文本只在 speech_context；
6. 独占“凤凰聚焦”片头主标题且紧邻目标正片，keep；
7. 同一名字出现在手机菜单，drop；
8. 通用凤凰 logo/版权声明 + target speech，drop；
9. 目标角标残留但 content_role 已是 platform_promo，drop；
10. 250–260 目标、260–270 同演播室职员表、270–280 版权页：前两段 keep，版权及后续 drop；
11. 120 秒连续 undecided 不得被单侧无限 bridge；
12. hard drop 阻断 bridge；
13. 0.032 秒尾碎片继承上一 hard drop；
14. LLM 会收到冲突 keep，但不能推翻 hard drop；
15. build_plan 中两个 keep 夹 1 秒明确 ad，即使 merge_gap=1.5 也必须生成两个 clip；
16. 所有 clip 与 hard drop overlap 为 0。

### 9.3 新增真实问题 fixture

从本次归档提取最小、可提交、去路径化 fixture：

- tests/fixtures/full_concat_phoenix_focus_head.json：source 2 的 39–81 秒 ASR owner、50–70 秒 visual/frame_shots。

- tests/fixtures/full_concat_phoenix_focus_tail.json：source 7 的 250–300.032 秒 visual/frame_shots 和预期标签。

- tests/fixtures/asr_overlap_10s_regression.json：三个 overlap window 与三个 10 秒 chunk。

fixture 不包含视频文件和用户路径，只保留复现算法所需字段。

### 9.4 缓存测试

扩展 tests/test_task_id_modes.py，并新增 common cache 测试：

1. contract 变化时 common key 必须变化；
2. Web 和 runner 对同一规范化输入生成同一个 key；
3. request 已是 10/5，但 contract 变化仍必须 rekey；
4. profile hash 不一致时 _common_analysis_ready=false；
5. source summary profile 不一致不得复用；
6. source_aggregate hash 变化时 full_concat 下游全部 stale；
7. _mark_running 后 _can_reuse 仍能读取正确 cache candidate；
8. vision ASR contract 改变时，--chunk/--failed-only 不复制旧结果。

### 9.5 工作流与 QC 测试

1. workflow_registry 的 full_concat 两条路径都包含 boundary_refine、plan_qc 和 output_qc；
2. full_concat_render 必须依赖 plan_qc；
3. full_concat_output_qc 必须位于 render 之后并读取实际 MP4；
4. high-risk plan finding 阻断 render；
5. output QC fail 时 MP4 标 quarantined，publishable=false，不能成为 latest video；
6. QC 报告能把输出 26:28 映射回 source 7 约 291.44 秒；
7. 无风险任务继续正常出片；
8. boundary refine 失败时使用粗边界并标 fallback，不能静默伪装高置信；
9. clip 与 hard drop 相交时 plan 直接失败；
10. output QC 不得只复用 detector 的 JSON 结论。

### 9.6 真实归档回放

用本次 source_aggregate/v1/source_aggregate.json 做离线 replay，不调用 ASR/vision：

- v5 基线应能复现错误；
- v6 应满足：
  - source 2 50–60 不再 keep；
  - source 2 60–约 65 sponsor drop，新闻部分保留；
  - source 7 270–300.032 全部从最终成片排除；
  - source 7 260–270 目标职员表保留；
  - split_parent_count 大于 0；
  - hard_drop_overlap_seconds=0；
  - 输出最后一个 clip 不再是 290.4–300.032。

最终还需 fresh common v2 全链路重跑。旧归档 replay 只能验证 detector 兼容路径，不能替代新 ASR owner 契约验收。

## 十、分阶段实施顺序

### P0：先固化失败样本和观测字段

改动：

- 提取三份最小 fixture；
- 增加 input_chunk_count、expanded_segment_count、split_parent_count、legacy_evidence_count、conflict_review_count、hard_drop_seconds、hard_drop_overlap_seconds、bridge_chain_max_seconds；
- 加入 v5 基线复现测试。

完成标准：

- 测试能稳定重现 source 2 和 source 7 的错误；
- 明确证明 10 秒路径 split_parent_count=0。

为什么先做：

- 防止再次出现“单测通过、生产新逻辑根本没运行”。

### P1：修 ASR owner 与 common contract

改动：

- analysis_contracts.py；
- ASR core/decode/owner；
- 三个消费点；
- timeline/digest 透传；
- common profile/key；
- summary 复用与 step cache 修复。

完成标准：

- 50–60 秒只获得 owner 49–61 window 的 owned_text；
- 新 common key 与旧池隔离；
- fresh common 产物可审计。

注意：

- 这一阶段会触发一次完整 common 重算；
- 最新任务 source_analysis 总耗时约 3955.864 秒，可作为资源评估参考，不作为固定 SLA。

### P2：视觉 ownership schema 与 v6 分类器

改动：

- vision prompt/schema/materializer；
- timeline 透传；
- 10 秒拆分；
- 四态分类；
- 目标片尾候选；
- 有限 bridge；
- LLM 冲突复审；
- build_plan 区间减法。

完成标准：

- 头尾 fixture 全部得到预期标签；
- 目标片头、主持人收尾和职员表保留；
- hard drop 无法被 speech、LLM、bridge 或 merge 重新救回。

### P3：边界细化、计划 QC 和实际成片 QC

改动：

- ad_boundary_refine；
- full_concat_plan_qc；
- full_concat_output_qc；
- workflow 和报告；
- 选择性 0.5–1 秒补帧。

完成标准：

- sponsor/news 混合段的切点不再只靠 5 秒关键帧和固定 0.4 秒 margin；
- 四张截图对应画面全部消失；
- 高风险计划会阻断 render；
- 实际 MP4 语义复核失败时成片会被隔离，不能发布。

### P4：灰度发布

建议按以下顺序：

1. shadow：v5 正常出片，同时离线生成 v6 detection/plan，不影响用户结果；
2. 10%：只给内部/测试任务启用 v6 render；
3. 50%：观察误删、漏删、时长和成本；
4. 100%：满足验收门槛后切换默认；
5. 保留 v5 feature flag 一个发布周期，但 v5 只作紧急回滚，不再作为质量合格版本。

## 十一、监控、报告与告警

每个任务至少记录：

| 指标 | 目的 |
|---|---|
| input_chunk_count / expanded_segment_count / split_parent_count | 确认 10 秒拆分真实执行 |
| asr_owner_missing_count / foreign_owner_segment_count | 发现 ASR 契约回退或串段 |
| hard_drop_seconds / hard_drop_overlap_seconds | 确认明确广告未进入计划 |
| mention_only_keep_count | 该值应为 0 |
| conflict_review_count / llm_override_count | 观察规则冲突和 LLM 行为 |
| bridge_chain_max_seconds | 防止 bridge 再次无限生长 |
| boundary_refined_count / fallback_count | 判断补帧是否工作 |
| plan_qc_high_count / output_qc_high_count | 两级高风险分别可见，阻断点可追溯 |
| head_risk_count / tail_risk_count | 直接覆盖本次问题 |
| output_expected_delta | 继续监控渲染准确性，但不把它当语义正确性 |

建议告警：

- split_parent_count=0 且存在 mixed frame role；
- foreign_owner_segment_count>0；
- mention_only_keep_count>0；
- hard_drop_overlap_seconds>0；
- plan_qc_high_count>0 但 render 仍成功；
- output_qc_high_count>0 但 publishable 仍为 true；
- common profile hash 与任务期望不一致。

## 十二、兼容、迁移与回滚

### 12.1 旧任务

旧 ASR v1 已经丢失 owner，不能伪装成可靠 v2。

两种方式：

- 正式生产验收：从 asr 开始全量重跑；
- 临时低成本回放：用 midpoint 唯一归属，标 legacy_midpoint_recovered 和 strict_evidence_eligible=false。

临时回放不得作为上线验收。

### 12.2 推荐重跑范围

可保留：

- source_prepare；
- metadata；
- source_quality_check；
- audio_extract；
- frame_extract；
- chunk_build。

必须重跑：

- asr；
- asr_digest；
- vision；
- timeline；
- timeline_digest；
- source_analysis/source_aggregate；
- ad_detection；
- ad_boundary_refine；
- full_concat_plan；
- full_concat_plan_qc；
- full_concat_render；
- full_concat_output_qc；
- 最终轨道/时长 QC。

vision 必须重跑，因为它当前把 ASR 摘要作为输入；只重跑 ad_detection 会继续使用旧污染视觉上下文。

### 12.3 回滚

配置保留：

    detector_version = "v5" | "v6"
    enable_boundary_refine = true | false
    plan_qc_mode = "shadow" | "warn" | "block"
    output_qc_mode = "shadow" | "warn" | "block"

回滚原则：

- common pool 按 contract key 隔离，不删除 v5/v6 任一目录；
- 只回滚 detector 时仍保持新 ASR owner，避免重新引入跨窗污染；
- plan/output QC 可从“阻断”临时降为“告警”，但 hard_drop interval subtraction 不应关闭；
- 回滚后必须在任务报告中标 detector_version 和降级项，不能静默。

## 十三、风险与应对

| 风险 | 应对 |
|---|---|
| 目标片头因强删除规则被误删 | 目标片头要求独占主标题或本地节目身份；边界候选结合连续强目标 anchor |
| ASR 边界词只在 overlap 中出现 | overlap 保留为 context，但不作为相邻 chunk 的硬证据 |
| 当前 FunASR 时间戳粗 | 标 window_coarse；不把父 speech 复制到视觉子段；边界依靠画面补帧 |
| vision 新字段偶尔缺失 | schema 校验 + legacy_low fallback；弱字段不能升级为强 keep |
| 新 common key 增加成本 | 一次性重算；按新 key 继续单槽锁；边界只选择性补帧 |
| LLM 误把宣传判 target | hard drop 不可被 LLM 翻转；target 返回必须有 ownership evidence |
| bridge 导致新闻尾句丢失 | 保留短 edge extension，但限制 5–8 秒并要求 target-like 证据 |
| merge_gap 把短广告并回 | 先做 hard drop 区间减法，gap 内有 drop 永不合并 |
| Web 与 runner key 不一致 | 统一 common_cache.py，不再复制两套实现 |
| 只改 prompt 却复用旧 vision | prompt/schema/ASR contract 全部进 profile 和 common key |

## 十四、文件改动清单

| 文件 | 主要修改 | 对应测试 |
|---|---|---|
| newsclip_agent/analysis_contracts.py（新增） | 契约版本、analysis profile | common key/version tests |
| newsclip_agent/common_cache.py（新增） | Web/runner 共用 key builder | Web 与 runner key 一致 |
| newsclip_agent/full_concat_boundary.py（新增） | 风险窗口、补帧、稳定转场和边界应用 | boundary refinement tests |
| newsclip_agent/full_concat_qc.py（新增） | 计划 QC、实际 MP4 抽检、发布门禁结果 | plan/output QC tests |
| newsclip_agent/pipeline.py | ASR owner、三处聚合、vision 物化、timeline 透传、source profile、边界/两级 QC 步骤、cache candidate | ASR、pipeline、workflow、cache integration |
| newsclip_agent/ad_detection.py | 10 秒拆分、Evidence、四态分类、片尾候选、有限 bridge、LLM review、区间减法、stats | head/tail/bridge/plan regression |
| newsclip_agent/prompt_texts/vision_chunk.txt | content_role/program_identity/program_mentions/frame schema | schema materialize tests |
| newsclip_agent/prompt_texts/ad_detection.txt | 归属与提及、hard drop 优先、冲突复审 | fake LLM payload/guard tests |
| newsclip_agent/workflow_registry.py | boundary_refine、plan_qc、render、output_qc 顺序/依赖 | workflow registry tests |
| config.toml | v6、bridge、refine、两级 QC 配置 | config loading tests |
| web_app.py | 统一 common key；报告暴露两级 QC；隔离失败 MP4；完成/最新成片门禁 | API/task mode tests |
| run_multisource_pipeline.py | 无条件重算 expected profile/key；ready 校验；下游 stale | common reuse/rekey tests |
| tests/test_asr_attribution.py（新增） | owner/core/decode/legacy midpoint | 10 秒 overlap 回归 |
| tests/test_ad_detection.py | 真实 10 秒、ownership、bridge、interval subtraction | 头尾所有已知错误 |
| tests/test_task_id_modes.py | contract 变化 rekey | common pool 隔离 |
| tests/test_full_concat_boundary_refinement.py（新增） | 稳定转场、fallback、源边界、区间合法性 | 边界精修 |
| tests/test_full_concat_workflow.py（新增） | 新步骤顺序、依赖、阻断和 quarantine | 工作流门禁 |
| tests/test_full_concat_report_api.py（新增） | 两级 QC 报告、latest video、legacy 兼容 | Web/API |
| tests/fixtures/full_concat_*（新增） | 本次最小真实 fixture | archive replay |

## 十五、开发完成定义

只有同时满足以下条件才算“问题解决”，不能以“又加了一轮规则”作为完成：

1. 本次真实 9 源任务用 fresh common v2 全链路跑通；
2. 四张问题截图对应画面从成片中消失；
3. 目标新闻首句、主持人收尾和确认属于目标栏目的职员表未丢；
4. ASR owner、视觉 ownership、四态决策均写入产物并可审计；
5. 真实 10 秒路径有拆分统计，不再出现 0 个 _frame_ 子段；
6. hard_drop_overlap_seconds=0；
7. mention_only_keep_count=0；
8. bridge_chain_max_seconds 不超过配置；
9. plan QC 对头尾和孤立 keep 完成检查，高风险会阻断渲染；
10. common contract 改变后一定生成新 key，旧池不会静默复用；
11. 新增回归、缓存、工作流测试通过，现有 29 个 ad_detection 测试继续通过或按新语义明确更新；
12. output QC 直接检查最终 MP4，失败成片 quarantined 且不作为 latest；
13. 最终报告同时给出 plan QC、output QC 与传统时长/轨道 QC，全部通过才显示“完成”。

## 十六、建议的最终实施优先级

必须按依赖顺序实施：

    真实失败 fixture 与统计
      → ASR owner + common contract
      → vision ownership schema 和透传
      → 10 秒拆分且不复制父 speech
      → 四态分类 + 目标边界候选 + 有限 bridge
      → build_plan 区间减法
      → 选择性边界细化
      → plan QC
      → render
      → output QC
      → fresh common 全链路回放与灰度

最不应该做的是先继续补“凤凰资讯、凤凰全球连线、华润”等关键词。关键词可以作为 hard-drop 的辅助证据，但无法修复时间归属、视觉证据丢失、菜单提及误判、生产拆分未执行和缓存静默复用这五个结构性问题。
