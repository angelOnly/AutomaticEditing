请读取 GitHub 仓库实际代码，优先按路径读取文件，不要只依赖搜索，不要以 README.md 为主要依据。

仓库：
https://github.com/angelOnly/AutomaticEditing
分支：
clean-highlight-reassembly
入口：
run_web.py

请先读取：
- run_web.py
- web_app.py
- config.toml
- run_pipeline.py
- newsclip_agent/pipeline.py

再沿着 import 和调用关系，只追踪和我提出的问题直接相关的模块。

分别读代码和给到的图片，文件资源

本轮只做第一阶段：
第一阶段你的输出只围绕用户提出的问题展开，不需要完整介绍整个项目。

不要完整介绍项目结构。
不要逐个解释文件作用。
不要给具体代码修改方案、代码片段、patch 或开发指南。

不能确认的地方请明确说不确定，不要猜。


最后停下来等我确认。
我确认后，你再进入第二阶段，给详细技术实现方案和代码开发指南。

只有当用户明确回复类似下面内容时，才进入第二阶段：
- 确认
- 方案没问题
- 继续
- 开始给开发方案
- 给我具体怎么改
- 给代码开发指南


第二阶段才可以输出：
1. 需要修改哪些文件
2. 每个文件具体怎么改
3. 关键函数怎么调整
4. 配置项怎么新增或修改
5. 如何兼容现有 Web 启动方式 python run_web.py
6. 关键伪代码或可直接替换的代码片段
7. 开发顺序
8. 测试步骤
9. 回滚方案
10. 风险点和兼容性说明

====================
重要要求
====================

- 必须基于实际代码分析，不要泛泛而谈。
- 不要只依赖 GitHub 搜索，优先按路径读取文件。
- 不要读取 README.md 作为主要依据。
- 第一阶段必须克制，只分析用户提出的问题。
- 第一阶段不要介绍整个项目。
- 第一阶段不要解释每个文件的作用。
- 没有用户确认前，不得输出详细代码修改方案。
- 如果代码中某些调用链没有完全确认，必须明确标注“不确定，需要继续追踪”，不要猜。