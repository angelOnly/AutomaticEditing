对，这 3 个都要修，而且要一起修，不能只修其中一个。

你现在这次日志的关键证据是：

```text
执行步骤: source_analysis, source_aggregate
...
完成: source_analysis (success)
完成: source_aggregate
...
RuntimeError: common analysis finished but source_aggregate is not ready;
debug={
  "source_prepare": {"status": "success", "output": "input/source_manifest.json", "output_exists": false},
  "source_analysis": {"status": "success", "output": "source_analysis/v1/source_analysis.json", "output_exists": false},
  "source_quality_check": {"status": null, "output": null, "output_exists": false},
  "source_aggregate": {"status": "success", "output": "source_aggregate/v1/source_aggregate.json", "output_exists": true}
}
```

这说明当前失败不是 ASR、vision、source_aggregate 本身失败，而是 **common wrapper 的 ready 判断和实际步骤状态不同步**。实际 common 只跑了 `source_analysis, source_aggregate`，但 wrapper 还把 `source_quality_check` 当成顶层必需步骤检查，所以它永远是 `null`，最后误判 common 没 ready。

同时，你前面指出的也对：**common wrapper 只返回 0/1、Web 不知道具体失败步骤，这个也必须修。**如果不修，就算这次解决了 `source_quality_check`，下次换成 `highlight_detection`、`reassembly_cut_plan`、`tts` 报错，页面还是只能看到一个笼统失败，无法从错误步骤恢复。

下面按你的 3 个任务重新整理成一版完整方案。

---

# 总体方案

顶层 common 流程保持简单：

```text
source_prepare
source_analysis
source_aggregate
```

但 `source_quality_check` 不删除，而是移动到 `source_analysis` 内部，作为每个源的第一个阶段：

```text
source_analysis
  ├─ source_001
  │   ├─ source_quality_check
  │   ├─ metadata
  │   ├─ audio_extract
  │   ├─ frame_extract
  │   ├─ chunk_build
  │   ├─ asr
  │   ├─ asr_digest
  │   ├─ vision
  │   ├─ timeline
  │   └─ timeline_digest
  ├─ source_002
  ├─ source_003
  └─ source_004
```

同时修复 wrapper：

```text
1. wrapper 不再要求 source_quality_check 顶层 ready
2. wrapper 遇到 pipeline_main 返回 1 时，读取 common manifest / step_status / source_status，找出真实失败步骤
3. wrapper 把失败挂回 child task manifest
4. Web 根据 failed_step 显示“从错误步骤重跑”按钮
```

---

# 任务 1：解决 source_quality_check 报错 / 状态卡住问题

## 当前原因

这次不是 `source_quality_check` 执行失败，而是它**没有作为顶层步骤执行**。

日志显示：

```text
执行步骤: source_analysis, source_aggregate
```

但 wrapper debug 还在检查：

```json
"source_quality_check": {"status": null}
```

所以是 **顶层 common step 定义不一致**。

---

## 修改 1：`newsclip_agent/workflow_registry.py`

把 unified common 顶层步骤改成只包含：

```python
UNIFIED_SOURCE_COMMON_REUSABLE_STEPS = [
    "source_prepare",
    "source_analysis",
    "source_aggregate",
]
```

同时 unified workflows 删除顶层 `source_quality_check`。

### 修改前

```python
("highlight_reassembly", True): [
    "source_prepare",
    "source_analysis",
    "source_quality_check",
    "source_aggregate",
    ...
]
```

### 修改后

```python
("highlight_reassembly", True): [
    "source_prepare",
    "source_analysis",
    "source_aggregate",
    "video_understanding",
    "highlight_detection",
    "highlight_reassembly_plan",
    "reassembly_cut_plan",
    "news_quality_gate",
    "news_quality_ai_review",
    "reassembly_render",
]
```

AI 配音模式同理：

```python
("ai_voiceover", True): [
    "source_prepare",
    "source_analysis",
    "source_aggregate",
    "content_analysis",
    "short_video_edit_plan",
    "voiceover_script",
    "voiceover_quality_gate",
    "tts",
    "subtitles",
    "cut_plan",
    "news_quality_gate",
    "news_quality_ai_review",
    "render",
]
```

依赖也要改：

```python
"source_aggregate": ["source_analysis"],
```

不要再写：

```python
"source_aggregate": ["source_quality_check"],
```

否则 manifest 语义还是冲突。

---

## 修改 2：`newsclip_agent/pipeline.py`

在 `step_source_analysis()` 的 `analyze_one()` 内部最前面加入源级质量检查。

### 新增 helper

放到 `PipelineRunner` 类里：

```python
def _quality_check_source_item(
    self,
    item: dict[str, Any],
    source_path: Path,
) -> dict[str, Any]:
    try:
        metadata = ffprobe_json(source_path)
    except Exception as exc:
        metadata = {"probe_error": str(exc)}

    if item.get("duration_seconds") is not None:
        metadata.setdefault("duration", item.get("duration_seconds"))
        metadata.setdefault("duration_seconds", item.get("duration_seconds"))

    metadata.setdefault("source_id", item.get("source_id", ""))
    metadata.setdefault("source_index", item.get("source_index"))
    metadata.setdefault("display_name", item.get("display_name", ""))
    metadata.setdefault("original_path", item.get("original_path", ""))

    report = build_source_quality_report([metadata])

    return {
        "version": "source_quality_check_internal_v1",
        "source_id": item.get("source_id", ""),
        "source_index": item.get("source_index"),
        "display_name": item.get("display_name", ""),
        "original_path": item.get("original_path", ""),
        "metadata": metadata,
        "report": report,
        "is_pass": bool(report.get("is_pass", False)),
        "reason": report.get("reason", ""),
        "checked_at": now_iso(),
    }
```

### 在 `analyze_one()` 里插入

找到：

```python
source_path = self._resolve_source_item_path(item)
```

后面插入：

```python
source_progress[source_id].update({
    "status": "running",
    "current_stage": "source_quality_check",
    "message": "素材质量检查中",
    "updated_at": now_iso(),
})

quality_doc = self._quality_check_source_item(item, source_path)
quality_path = write_json(source_dir / "source_quality_check.json", quality_doc)

if not quality_doc.get("is_pass"):
    elapsed = round(time.perf_counter() - started_at, 3)
    reason = quality_doc.get("reason") or "source quality check failed"

    write_json(source_status_path, {
        "source_id": source_id,
        "status": "failed",
        "failed_stage": "source_quality_check",
        "error": reason,
        "quality_check": relpath(quality_path, self.task_dir),
        "updated_at": now_iso(),
        "elapsed_seconds": elapsed,
    })

    source_progress[source_id].update({
        "status": "failed",
        "current_stage": "source_quality_check",
        "error": reason,
        "quality_check": relpath(quality_path, self.task_dir),
        "updated_at": now_iso(),
    })

    raise RuntimeError(f"source quality check failed for {source_id}: {reason}")
```

成功返回时加上：

```python
"quality_check": relpath(quality_path, self.task_dir),
```

这样质量检查不再是顶层 common step，而是每个源自己的预检查。失败也会归因到具体源。

---

## 修改 3：`newsclip_agent/media_quality.py`

这个必须修。因为现在质量检查真的会跑，不能只依赖不存在的 `has_video / has_audio`。

改成兼容三种来源：

```python
from __future__ import annotations

from typing import Any


def _has_stream(metadata: dict[str, Any], stream_type: str) -> bool:
    streams = metadata.get("streams") or []
    if not isinstance(streams, list):
        return False
    return any(
        isinstance(stream, dict) and stream.get("codec_type") == stream_type
        for stream in streams
    )


def _duration_seconds(metadata: dict[str, Any]) -> float:
    for key in ("duration_seconds", "duration"):
        try:
            value = float(metadata.get(key) or 0)
            if value > 0:
                return value
        except Exception:
            pass

    fmt = metadata.get("format") or {}
    if isinstance(fmt, dict):
        try:
            return float(fmt.get("duration") or 0)
        except Exception:
            return 0.0

    return 0.0


def build_source_quality_report(sources_metadata: list[dict[str, Any]]) -> dict[str, Any]:
    if not sources_metadata:
        return {"is_pass": False, "reason": "No sources provided for quality check."}

    checked_sources: list[dict[str, Any]] = []

    for index, metadata in enumerate(sources_metadata, start=1):
        if not isinstance(metadata, dict) or not metadata:
            return {"is_pass": False, "reason": f"Source {index} metadata is empty."}

        if metadata.get("probe_error"):
            return {
                "is_pass": False,
                "reason": f"Source {index} ffprobe failed: {metadata.get('probe_error')}",
            }

        has_video = bool(
            metadata.get("has_video")
            or metadata.get("video_codec")
            or _has_stream(metadata, "video")
        )

        has_audio = bool(
            metadata.get("has_audio")
            or metadata.get("audio_codec")
            or _has_stream(metadata, "audio")
        )

        duration = _duration_seconds(metadata)

        if not has_video and not has_audio:
            return {"is_pass": False, "reason": f"Source {index} has neither video nor audio streams."}

        if not has_video:
            return {"is_pass": False, "reason": f"Source {index} has no video stream."}

        if not has_audio:
            return {
                "is_pass": False,
                "reason": f"Source {index} has no audio stream. Audio is required for ASR analysis.",
            }

        if duration < 5:
            return {
                "is_pass": False,
                "reason": f"Source {index} is too short ({duration:.1f}s). Minimum required duration is 5 seconds.",
            }

        width = int(metadata.get("width") or 0)
        height = int(metadata.get("height") or 0)

        if width and height and (width < 320 or height < 240):
            return {
                "is_pass": False,
                "reason": f"Source {index} resolution is too low ({width}x{height}).",
            }

        checked_sources.append({
            "source_index": index,
            "source_id": metadata.get("source_id", ""),
            "display_name": metadata.get("display_name", ""),
            "has_video": has_video,
            "has_audio": has_audio,
            "duration_seconds": round(duration, 3),
            "width": width,
            "height": height,
            "video_codec": metadata.get("video_codec", ""),
            "audio_codec": metadata.get("audio_codec", ""),
        })

    return {
        "is_pass": True,
        "reason": "",
        "checked_sources": checked_sources,
    }
```

---

## 修改 4：`newsclip_agent/utils.py`

`ffprobe_json()` 返回标准字段：

```python
"has_video": bool(v_stream),
"has_audio": bool(a_stream),
"duration_seconds": duration,
```

关键片段：

```python
streams = data.get("streams", []) or []
v_stream = next((s for s in streams if s.get("codec_type") == "video"), {})
a_stream = next((s for s in streams if s.get("codec_type") == "audio"), {})

has_video = bool(v_stream)
has_audio = bool(a_stream)

duration = float(
    data.get("format", {}).get("duration")
    or v_stream.get("duration")
    or a_stream.get("duration")
    or 0
)

return {
    "duration": duration,
    "duration_seconds": duration,
    "duration_timecode": seconds_to_timecode(duration, ms=True),
    "has_video": has_video,
    "has_audio": has_audio,
    "width": int(v_stream.get("width") or 0),
    "height": int(v_stream.get("height") or 0),
    "fps": round(fps, 3),
    "video_codec": v_stream.get("codec_name", ""),
    "audio_codec": a_stream.get("codec_name", ""),
    "format": data.get("format", {}),
    "streams": streams,
}
```

---

# 任务 2：按源分析加进度

当前 Web 只知道 `source_analysis` 运行中，但不知道：

```text
第几个源
当前源跑到 metadata / asr / vision / timeline 哪一步
vision chunk 完成多少
ASR chunk 完成多少
```

日志里确实一直在打，比如：

```text
ASR: transcribing chunk_0002 2/5
vision chunk_0004: success
完成: vision
完成: timeline
完成: timeline_digest
```

但这些没有结构化同步给前端。

---

## 修改 1：`pipeline.py` 内部维护 `source_progress`

`step_source_analysis()` 里已有 `source_progress`，但它粒度不够。要增加字段：

```json
{
  "source_id": "source_001",
  "source_index": 1,
  "display_name": "...",
  "status": "running",
  "current_stage": "asr",
  "current_stage_label": "语音转文字",
  "completed_chunks": 2,
  "total_chunks": 5,
  "message": "ASR 2/5",
  "updated_at": "..."
}
```

---

## 修改 2：不要把所有提交线程都标 running

现在代码提交 future 时，把所有 source 都标 running，这会误导。

改成：

```python
for idx, item in enumerate(sources, start=1):
    source_id = source_id_for(item)
    source_progress[source_id].update({
        "status": "pending",
        "updated_at": now_iso(),
    })
    future_map[executor.submit(analyze_one, item)] = item
```

然后在 `analyze_one()` 开始时才标 running：

```python
source_progress[source_id].update({
    "status": "running",
    "current_stage": "source_quality_check",
    "message": "素材质量检查中",
    "updated_at": now_iso(),
})
```

---

## 修改 3：同步 child runner 的 manifest

`source_analysis` 内部每个源是一个 child `PipelineRunner`。它自己的 manifest 会更新每个步骤状态。外层要定时读取 child manifest，把当前 running step 同步回 `source_progress`。

在 `pipeline.py` 顶部加：

```python
import threading
```

在 `analyze_one()` 创建 child_runner 后，`child_runner.run()` 前加：

```python
stop_child_sync = threading.Event()

def sync_child_progress() -> None:
    while not stop_child_sync.is_set():
        try:
            child_manifest = read_json(child_runner.manifest_path, {})
            child_steps = child_manifest.get("steps", {}) or {}

            running_step = ""
            running_item = {}
            for name, value in child_steps.items():
                if isinstance(value, dict) and value.get("status") == "running":
                    running_step = name
                    running_item = value
                    break

            if running_step:
                summary = running_item.get("summary") or {}
                source_progress[source_id].update({
                    "status": "running",
                    "current_stage": running_step,
                    "stage_summary": summary,
                    "message": _source_stage_message(running_step, summary),
                    "updated_at": now_iso(),
                })

                self._update_step_progress(
                    "source_analysis",
                    f"source {source_pos}/{total_sources}: {running_step}",
                    {
                        "completed_sources": sum(
                            1 for v in source_progress.values()
                            if v.get("status") in {"success", "failed"}
                        ),
                        "total_sources": total_sources,
                        "success_count": sum(1 for v in source_progress.values() if v.get("status") == "success"),
                        "failed_count": sum(1 for v in source_progress.values() if v.get("status") == "failed"),
                        "running_count": sum(1 for v in source_progress.values() if v.get("status") == "running"),
                        "source_progress": list(source_progress.values()),
                        "source_max_workers": active_workers,
                        "per_source_vision_max_workers": per_source_vision_workers,
                    },
                )
        except Exception:
            pass

        stop_child_sync.wait(2.0)

sync_thread = threading.Thread(target=sync_child_progress, daemon=True)
sync_thread.start()

try:
    child_manifest = child_runner.run()
finally:
    stop_child_sync.set()
```

再加一个小函数：

```python
def _source_stage_message(step: str, summary: dict[str, Any]) -> str:
    if step == "vision":
        total = int(summary.get("total_chunks") or 0)
        processed = int(summary.get("processed_chunks") or 0)
        failed = int(summary.get("failed_chunks") or 0)
        if total:
            return f"画面识别 {processed}/{total}" + (f"，失败 {failed}" if failed else "")
    if step == "asr":
        total = int(summary.get("total_chunks") or 0)
        processed = int(summary.get("processed_chunks") or 0)
        if total:
            return f"语音转文字 {processed}/{total}"
    return step
```

---

## 修改 4：`web_static/app.js` 显示源级进度

`UNIFIED_*_STEPS` 删除顶层 `source_quality_check`，但 `STEP_LABELS` 保留：

```javascript
source_quality_check: "素材质量检查",
```

在 `stepNote()` 里加：

```javascript
if (step === "source_analysis") {
  const completed = Number(item.completed_sources || 0);
  const total = Number(item.total_sources || 0);
  const success = Number(item.success_count || 0);
  const failed = Number(item.failed_count || 0);
  const running = Number(item.running_count || 0);

  if (total) {
    const activeSources = (item.source_progress || [])
      .filter((source) => source.status === "running")
      .slice(0, 2)
      .map((source) => {
        const name = source.display_name || source.source_id || "素材";
        const stage = source.current_stage ? `：${stepLabel(source.current_stage)}` : "";
        const message = source.message ? ` ${source.message}` : "";
        return `${name}${stage}${message}`;
      });

    const activeText = activeSources.length ? `；${activeSources.join("；")}` : "";
    return `按源分析 ${completed}/${total}，成功 ${success}，失败 ${failed}，运行 ${running}${activeText}`;
  }
}
```

效果：

```text
按源分析 1/4，成功 1，失败 0，运行 2；素材A：语音转文字 ASR 3/9；素材B：画面识别 4/5
```

---

# 任务 3：恢复“从错误步骤开始重新运行”按钮

这个必须修 common wrapper 和 Web 两边。

现在 `pipeline.main()` 遇到异常只返回 1：

```python
except Exception as exc:
    ...
    return 1
```

外层 `run_multisource_pipeline.py` 只拿到 return code，于是只能报：

```text
common analysis failed with return code 1
```

这会导致 Web 不知道哪个 step 失败，也就无法给出正确重跑按钮。

---

## 修改 1：`run_multisource_pipeline.py` 增加失败定位函数

新增：

```python
def _find_failed_common_step(common_dir: Path) -> tuple[str, str]:
    manifest = read_json(common_dir / "manifest.json", {})
    steps = manifest.get("steps", {}) or {}

    for step in COMMON_REUSABLE_STEPS:
        item = steps.get(step) or {}
        if item.get("status") == "failed":
            return step, str(
                item.get("error")
                or item.get("reason")
                or manifest.get("user_message")
                or f"{step} failed"
            )

    # source_analysis 内部源失败
    source_analysis = steps.get("source_analysis") or {}
    output = source_analysis.get("output")
    if output:
        analysis = read_json(common_dir / output, {})
        for failure in analysis.get("failed_sources") or []:
            if isinstance(failure, dict):
                msg = failure.get("error") or failure.get("reason") or "source analysis failed"
                return "source_analysis", str(msg)

    for step in COMMON_REUSABLE_STEPS:
        item = steps.get(step) or {}
        if item.get("status") == "running":
            return step, str(manifest.get("user_message") or f"{step} interrupted or failed")

    return "source_analysis", str(manifest.get("user_message") or "common analysis failed")
```

---

## 修改 2：`run_multisource_pipeline.py` 把失败写回 manifest

改 `_mark_common_failed()`：

```python
def _mark_common_failed(common_dir: Path, message: str, failed_step: str | None = None) -> None:
    manifest_path = common_dir / "manifest.json"
    manifest = read_json(manifest_path, {})
    now = datetime.now().isoformat(timespec="seconds")

    failed_step = failed_step or "source_analysis"

    manifest.setdefault("task_id", common_dir.name)
    manifest.setdefault("created_at", now)
    manifest["updated_at"] = now
    manifest["status"] = "failed"
    manifest["user_message"] = message
    manifest.setdefault("steps", {})

    step_item = dict(manifest["steps"].get(failed_step) or {})
    if step_item.get("status") not in {"success", "partial_success", "skipped"}:
        step_item.update({
            "step_name": failed_step,
            "status": "failed",
            "error": message,
            "updated_at": now,
            "can_rerun": True,
            "common_progress": True,
            "common_task_id": common_dir.name,
        })
        manifest["steps"][failed_step] = step_item

    manifest["common_wrapper_error"] = {
        "status": "failed",
        "message": message,
        "failed_step": failed_step,
        "updated_at": now,
    }

    write_json(manifest_path, manifest)
```

---

## 修改 3：`_ensure_common_analysis()` 处理 return code 1

把：

```python
result = pipeline_main(pipeline_args)
if result != 0:
    raise RuntimeError(f"common analysis failed with return code {result}")
```

改成：

```python
result = pipeline_main(pipeline_args)
if result != 0:
    failed_step, detail = _find_failed_common_step(common_dir)
    _mark_common_failed(common_dir, detail, failed_step=failed_step)
    raise RuntimeError(f"common analysis failed at {failed_step}: {detail}")
```

这样 wrapper 不再只是 0/1，而是能把错误转换成：

```json
{
  "common_wrapper_error": {
    "failed_step": "source_analysis",
    "message": "source quality check failed for source_003: ..."
  }
}
```

---

## 修改 4：`run_multisource_pipeline.py` ready 检查错误也要定位

你日志里的这次错误发生在 ready 检查：

```text
common analysis finished but source_aggregate is not ready
```

这个也要改。之前文档里也建议把它改成 `not_ready_steps`，避免误导成只有 `source_aggregate` 问题。

改成：

```python
not_ready_steps = [
    step
    for step in COMMON_REUSABLE_STEPS
    if not _common_step_ready(common_dir, step)
]

debug = {
    step: {
        "ready": _common_step_ready(common_dir, step),
        "status": (steps.get(step) or {}).get("status"),
        "output": (steps.get(step) or {}).get("output"),
        "output_exists": _common_step_output_exists(common_dir, step),
    }
    for step in COMMON_REUSABLE_STEPS
}

if not_ready_steps:
    failed_step = not_ready_steps[0]
    message = (
        "common analysis finished but reusable common steps are not ready; "
        f"not_ready_steps={not_ready_steps}; "
        f"debug={json.dumps(debug, ensure_ascii=False)}"
    )
    _mark_common_failed(common_dir, message, failed_step=failed_step)
    raise RuntimeError(message)
```

你现在的日志就是这类问题，改完后不会再误导为 `source_aggregate is not ready`，而会明确是哪几个 common artifact 不 ready。

---

## 修改 5：同步失败到 child task manifest

`run_multisource_pipeline.py` 已有 `_sync_common_progress_to_child()`，但它主要同步 common steps。要确保同步：

```python
if common_manifest.get("common_wrapper_error"):
    child_manifest["common_wrapper_error"] = common_manifest["common_wrapper_error"]
```

并且：

```python
if common_manifest.get("status") == "failed":
    child_manifest["status"] = "failed"
    child_manifest["user_message"] = common_manifest.get("user_message", "")
```

这部分当前代码已有部分逻辑，但要补 `common_wrapper_error`，否则前端看不到 failed_step。

---

## 修改 6：`web_static/app.js` 错误卡片兼容 `common_wrapper_error`

现在前端主要找：

```javascript
const failedEntry = visibleEntries.find(([, item]) => item.status === "failed");
```

如果 common wrapper 只写了 `manifest.status=failed`，没有 step failed，就不会显示按钮。

加 fallback：

```javascript
const wrapperError = manifest.common_wrapper_error || null;
const wrapperFailedStep = wrapperError?.failed_step || "source_analysis";
```

在 `renderJobError()` 里加：

```javascript
} else if (wrapperError || manifest.status === "failed") {
  const stepName = wrapperFailedStep;
  const rawError = wrapperError?.message || manifest.user_message || "任务失败，但没有记录到具体失败步骤。";
  const explained = explainStepError(stepName, rawError);

  box.innerHTML = `
    <strong>${escapeHtml(explained.title)}</strong>
    <p>${escapeHtml(explained.message)}</p>
    <p>${escapeHtml(explained.suggestion)}</p>
    <div class="error-actions">
      <button class="button primary rerun-btn" type="button">
        从 ${escapeHtml(stepLabel(stepName))} 重新运行
      </button>
    </div>
    <details>
      <summary>查看技术详情</summary>
      <pre>${escapeHtml(rawError)}</pre>
    </details>
  `;

  box.querySelector(".rerun-btn")?.addEventListener("click", () => {
    rerunFromSuggestedStep(stepName);
  });

  box.classList.remove("hidden");
  msgBox.classList.add("hidden");
```

这样按钮能恢复。

---

# 最终改动归纳

## 1. source_quality_check 问题

不是删除，而是：

```text
从 common 顶层 step 移到 source_analysis 内部每个源的第一步
```

同时修 `media_quality.py` 和 `ffprobe_json()`，保证它真的能正常判断。

---

## 2. 按源分析进度

`source_analysis` 的 manifest 里持续写：

```json
{
  "completed_sources": 1,
  "total_sources": 4,
  "success_count": 1,
  "failed_count": 0,
  "running_count": 2,
  "source_progress": [
    {
      "source_id": "...",
      "status": "running",
      "current_stage": "asr",
      "message": "语音转文字 3/9"
    }
  ]
}
```

前端 `stepNote()` 展示出来。

---

## 3. 错误步骤重跑按钮

`run_multisource_pipeline.py` 不再只暴露 return code 1，而是写：

```json
"common_wrapper_error": {
  "failed_step": "source_analysis",
  "message": "..."
}
```

前端根据 `failed_step` 显示：

```text
从 按源分析素材 重新运行
```

按钮调用现有 `rerunFromSuggestedStep(stepName)`。

---

# 这版才是完整方案

你提的 3 个问题对应关系是：

```text
source_quality_check 卡住
→ 修 workflow_registry 顶层 common 定义 + source_analysis 内部预检查

按源分析没进度
→ source_analysis 写 source_progress + 前端 stepNote 展示

错误后没有从错误步骤重跑按钮
→ wrapper 定位 failed_step + child manifest 同步 + 前端 common_wrapper_error fallback
```

这三个都要做。只改其中一个，项目还是会继续出现“日志在跑但页面不知道”“报错只有 return code 1”“无法从失败点继续”的问题。
