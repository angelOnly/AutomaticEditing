# 凤凰新闻视频智能拆条系统：多视频源拖拽剪辑开发方案

> 目标：在保持 `python run_web.py` 启动方式不变的前提下，让 Web 工作台支持本地素材、远程素材、多选/拖拽多个视频源，并将多个视频源作为一个剪辑任务输入，最终剪辑成一个视频或一组视频。

---

## 1. 代码现状结论

当前系统入口是 `run_web.py`，只负责寻找 7860 起始的可用端口并启动 `web_app:app`，因此 Web 启动方式无需改动。

当前 Web 请求模型 `RunRequest` 只支持单个 `input_video`、单个 `remote_video`、单个 `remote_video_id`。`run_pipeline()` 中通过 `_prepare_run_input_video(req)` 得到一个 `input_path`，再生成任务 ID、创建任务目录、拼接命令。`_build_run_command()` 最终只会给 `run_pipeline.py` 传一个 `--input <path>`。

当前流水线 `run_pipeline.py` 只是导入 `newsclip_agent.pipeline.main()`；真正逻辑都在 `newsclip_agent/pipeline.py`。`RunOptions` 里也只有 `input: str | None`，`parse_args()` 只有 `--input`，首次运行 `_init_task()` 要求必须有单个 `--input`，并将它复制/链接为任务目录下的 `input/source.xxx`。

当前 AI 配音模式和高光重组模式都依赖同一套单源预处理步骤：metadata、audio_extract、frame_extract、chunk_build、asr、vision、timeline。后续高光重组的渲染函数 `_render_reassembly_one()` 也只调用 `self._source_video()` 返回一个源视频，然后所有 clip 都从这个源视频上按 `source_start/source_end` 裁切。

所以当前项目不是“多源剪辑架构”，而是“单源视频流水线”。要支持多个视频拖拽，最稳的第一版是：把多个素材在任务启动前规范化合成为一个任务级虚拟源视频 `source_multi.mp4`，同时保存多源映射 `source_manifest.json`。这样既能兼容现有 pipeline，又能保留源素材来源、顺序、时长、虚拟时间轴偏移。

---

## 2. 推荐实现路线

### 2.1 V1 推荐：任务级虚拟源视频方案

多个输入源进入任务后，后端先做一个 `source_multi.mp4`：

1. 本地素材：直接解析为本地 path。
2. 远程素材：先走已有 `download_ucms_video()` 下载到本地缓存。
3. 按用户拖拽顺序做顺序拼接。
4. 每段统一规范化为同一分辨率、帧率、采样率，避免 concat 因编码不一致失败。
5. 生成：
   - `outputs/<task_id>/input/source_multi.mp4`
   - `outputs/<task_id>/input/source_manifest.json`

之后仍然调用现有命令：

```bash
python run_pipeline.py --input outputs/<task_id>/input/source_multi.mp4 --task-id <task_id> ...
```

现有 metadata/audio_extract/frame_extract/asr/vision/timeline/highlight_detection/tts/subtitles/render 都可以继续运行。

### 2.2 为什么不建议第一版直接改成真正多源 pipeline

真正多源 pipeline 需要每个 clip 带 `source_id`，并修改 metadata、audio、frames、chunks、asr、timeline、render 全链路。尤其 `_render_reassembly_one()` 和 `_render_one()` 目前都假设只有一个 `self._source_video()`。这会扩大改动面，也更容易破坏已经跑通的 AI 配音与高光重组模式。

V1 先用虚拟源视频满足业务：多个新闻素材剪成一个成片。后续再做 V2 多源原生渲染，把每个 clip 从原始素材裁切，而不是从拼接代理视频裁切。

---

## 3. 需要修改的文件

### 必改文件

1. `web_app.py`
2. `web_static/index.html`
3. `web_static/app.js`
4. `web_static/styles.css`
5. `newsclip_agent/pipeline.py`
6. 新增：`newsclip_agent/multisource.py`

### 可选修改

1. `config.toml`：增加多源拼接默认参数。
2. `README.md`：补充多源任务使用说明。

---

## 4. 后端设计

## 4.1 新增数据结构

在 `web_app.py` 里给 `RunRequest` 增加多源字段：

```python
class RunRequest(BaseModel):
    input_video: str | None = None
    input_videos: list[str] = Field(default_factory=list)

    remote_video: dict[str, Any] | None = None
    remote_videos: list[dict[str, Any]] = Field(default_factory=list)

    source_items: list[dict[str, Any]] = Field(default_factory=list)
```

字段说明：

- `input_video`：兼容原单本地素材。
- `input_videos`：兼容简单多本地素材。
- `remote_video`：兼容原单远程素材。
- `remote_videos`：兼容简单多远程素材。
- `source_items`：推荐新前端统一传这个，支持本地、远程、混合、排序。

推荐前端请求格式：

```json
{
  "production_mode": "highlight_reassembly",
  "source_items": [
    {"source_type": "local", "path": "videos/20260601_162933.mp4", "display_name": "20260601_162933"},
    {"source_type": "remote_ucms", "remote_video": {"remote_id": "...", "name": "..."}},
    {"source_type": "local", "path": "videos/20260601_163433.mp4", "display_name": "20260601_163433"}
  ],
  "aspect_ratio": "16:9",
  "chunk_seconds": 60,
  "frame_interval": 10
}
```

---

## 4.2 新增 `newsclip_agent/multisource.py`

新增文件职责：

- 接收多个本地视频 path。
- 用 ffprobe 获取每段时长、分辨率、音视频流信息。
- 生成规范化中间片段。
- 用 concat demuxer 合并。
- 写出 `source_manifest.json`。

建议代码：

```python
from __future__ import annotations

from dataclasses import dataclass
from pathlib import Path
from typing import Any

from .utils import ensure_dir, ffprobe_json, relpath, run_cmd, seconds_to_timecode, write_json, write_text

VIDEO_SUFFIXES = {".mp4", ".mov", ".mkv", ".m4v", ".avi"}

@dataclass
class MultiSourceItem:
    source_id: str
    source_type: str
    path: Path
    display_name: str = ""
    remote: dict[str, Any] | None = None


def build_multi_source_video(
    *,
    sources: list[MultiSourceItem],
    task_dir: Path,
    root_dir: Path,
    aspect_ratio: str = "16:9",
    fps: int = 25,
    sample_rate: int = 48000,
) -> dict[str, Any]:
    if len(sources) < 2:
        raise ValueError("build_multi_source_video requires at least 2 sources")

    input_dir = ensure_dir(task_dir / "input")
    normalized_dir = ensure_dir(input_dir / "normalized_sources")
    output_video = input_dir / "source_multi.mp4"
    concat_file = input_dir / "concat_sources.txt"
    manifest_file = input_dir / "source_manifest.json"

    timeline_sources: list[dict[str, Any]] = []
    normalized_files: list[Path] = []
    cursor = 0.0

    for index, item in enumerate(sources, start=1):
        source_path = item.path.resolve()
        if not source_path.exists() or not source_path.is_file():
            raise FileNotFoundError(f"source not found: {source_path}")
        if source_path.suffix.lower() not in VIDEO_SUFFIXES:
            raise ValueError(f"unsupported video type: {source_path.name}")

        metadata = ffprobe_json(source_path)
        duration = float(metadata.get("duration") or 0)
        if duration <= 0:
            raise ValueError(f"invalid duration: {source_path.name}")

        normalized = normalized_dir / f"source_{index:03d}.mp4"
        vf = _normalize_video_filter(aspect_ratio)
        run_cmd([
            "ffmpeg", "-y", "-hide_banner", "-loglevel", "error",
            "-i", str(source_path),
            "-vf", vf,
            "-r", str(fps),
            "-c:v", "libx264", "-preset", "veryfast", "-pix_fmt", "yuv420p",
            "-c:a", "aac", "-ar", str(sample_rate), "-ac", "2",
            "-movflags", "+faststart",
            str(normalized),
        ])
        normalized_files.append(normalized)

        normalized_meta = ffprobe_json(normalized)
        normalized_duration = float(normalized_meta.get("duration") or duration)
        timeline_sources.append({
            "source_id": item.source_id or f"src_{index:03d}",
            "source_index": index,
            "source_type": item.source_type,
            "display_name": item.display_name or source_path.name,
            "original_path": str(source_path),
            "normalized_file": relpath(normalized, task_dir),
            "original_duration_seconds": round(duration, 3),
            "duration_seconds": round(normalized_duration, 3),
            "virtual_start_seconds": round(cursor, 3),
            "virtual_end_seconds": round(cursor + normalized_duration, 3),
            "virtual_start": seconds_to_timecode(cursor, ms=True),
            "virtual_end": seconds_to_timecode(cursor + normalized_duration, ms=True),
            "remote": item.remote or {},
        })
        cursor += normalized_duration

    write_text(concat_file, "".join(f"file '{str(p).replace(chr(92), '/')}'\n" for p in normalized_files))
    run_cmd([
        "ffmpeg", "-y", "-hide_banner", "-loglevel", "error",
        "-f", "concat", "-safe", "0",
        "-i", str(concat_file),
        "-c", "copy",
        str(output_video),
    ])

    output_meta = ffprobe_json(output_video)
    manifest = {
        "source_mode": "multi_source_concat_proxy",
        "source_count": len(sources),
        "source_video": relpath(output_video, task_dir),
        "total_duration_seconds": round(float(output_meta.get("duration") or cursor), 3),
        "aspect_ratio": aspect_ratio,
        "fps": fps,
        "sample_rate": sample_rate,
        "sources": timeline_sources,
    }
    write_json(manifest_file, manifest)
    return {
        "input_video": output_video,
        "source_manifest": manifest_file,
        "manifest": manifest,
    }


def _normalize_video_filter(aspect_ratio: str) -> str:
    if aspect_ratio == "9:16":
        return "scale=1080:-2,pad=1080:1920:(ow-iw)/2:(oh-ih)/2:black,setsar=1"
    return "scale=1920:1080:force_original_aspect_ratio=decrease,pad=1920:1080:(ow-iw)/2:(oh-ih)/2:black,setsar=1"
```

---

## 4.3 修改 `web_app.py`

### 4.3.1 import

增加：

```python
from newsclip_agent.multisource import MultiSourceItem, build_multi_source_video
```

### 4.3.2 修改 RunRequest

在现有字段附近加入：

```python
input_videos: list[str] = Field(default_factory=list)
remote_videos: list[dict[str, Any]] = Field(default_factory=list)
source_items: list[dict[str, Any]] = Field(default_factory=list)
```

### 4.3.3 新增多文件上传接口

保留原 `/api/videos/upload`，新增：

```python
@app.post("/api/videos/upload-multiple")
async def upload_multiple_videos(files: list[UploadFile] = File(...)) -> list[dict[str, Any]]:
    results = []
    for file in files:
        suffix = Path(file.filename or "").suffix.lower()
        if suffix not in {".mp4", ".mov", ".mkv", ".m4v", ".avi"}:
            await file.close()
            raise HTTPException(400, f"unsupported video type: {file.filename}")
        ensure_dir(VIDEOS_DIR)
        target = _unique_video_path(_safe_filename(file.filename or f"video{suffix}"))
        try:
            with target.open("wb") as out:
                while chunk := await file.read(1024 * 1024):
                    out.write(chunk)
        finally:
            await file.close()
        results.append({
            "name": target.name,
            "path": relpath(target, ROOT),
            "size_mb": round(target.stat().st_size / 1024 / 1024, 2),
            "updated_at": datetime.fromtimestamp(target.stat().st_mtime).isoformat(timespec="seconds"),
        })
    return results
```

### 4.3.4 新增多源解析函数

```python
def _collect_source_items(req: RunRequest) -> list[dict[str, Any]]:
    if req.source_items:
        return req.source_items
    items: list[dict[str, Any]] = []
    if req.input_video:
        items.append({"source_type": "local", "path": req.input_video})
    for path in req.input_videos or []:
        items.append({"source_type": "local", "path": path})
    if req.remote_video:
        items.append({"source_type": "remote_ucms", "remote_video": req.remote_video})
    for remote in req.remote_videos or []:
        items.append({"source_type": "remote_ucms", "remote_video": remote})
    return items


def _resolve_run_sources(req: RunRequest) -> list[MultiSourceItem]:
    raw_items = _collect_source_items(req)
    if not raw_items:
        return []

    resolved: list[MultiSourceItem] = []
    for index, item in enumerate(raw_items, start=1):
        source_type = str(item.get("source_type") or item.get("type") or "local")
        if source_type in {"local", "video"}:
            path_value = item.get("path") or item.get("input_video")
            if not path_value:
                raise HTTPException(400, f"source_items[{index}].path is required")
            path = _resolve_input_video(str(path_value))
            resolved.append(MultiSourceItem(
                source_id=str(item.get("source_id") or f"src_{index:03d}"),
                source_type="local",
                path=path,
                display_name=str(item.get("display_name") or path.name),
            ))
            continue

        if source_type in {"remote", "remote_ucms"}:
            remote_video = item.get("remote_video") or item
            if not isinstance(remote_video, dict):
                raise HTTPException(400, f"source_items[{index}].remote_video is required")
            try:
                cached = download_ucms_video(
                    REMOTE_UCMS_CONFIG,
                    remote_video,
                    root_dir=ROOT,
                    force=req.force_remote_download,
                )
            except Exception as exc:
                raise HTTPException(502, f"remote video download failed: {exc}") from exc
            path = _resolve_input_video(str(cached["path"]))
            resolved.append(MultiSourceItem(
                source_id=str(item.get("source_id") or remote_video.get("remote_id") or remote_video.get("id") or f"src_{index:03d}"),
                source_type="remote_ucms",
                path=path,
                display_name=str(remote_video.get("display_name") or remote_video.get("name") or path.name),
                remote=remote_video,
            ))
            continue

        raise HTTPException(400, f"unsupported source_type: {source_type}")

    return resolved
```

### 4.3.5 重构 `run_pipeline(req)` 的 input_path 生成

把原逻辑：

```python
_prepare_mode_switched_run(req)
input_path = _prepare_run_input_video(req)
fingerprint = _run_fingerprint(req, input_path) if input_path else ""
input_name_for_task = _display_input_name(req)
...
task_dir = ensure_dir(OUTPUTS_DIR / task_id)
```

替换为：

```python
_prepare_mode_switched_run(req)
source_items = _resolve_run_sources(req)

if len(source_items) > 1:
    fingerprint = _multi_source_fingerprint(req, source_items)
    input_name_for_task = _display_multi_source_name(source_items)
    task_id = req.task_id.strip() if req.task_id else _make_task_id_from_fingerprint(input_name_for_task, req.production_mode, fingerprint)
    task_dir = ensure_dir(OUTPUTS_DIR / task_id)
    built = build_multi_source_video(
        sources=source_items,
        task_dir=task_dir,
        root_dir=ROOT,
        aspect_ratio=req.aspect_ratio,
        fps=int(PROJECT_CONFIG.raw.get("multi_source", {}).get("fps", 25)),
        sample_rate=int(PROJECT_CONFIG.raw.get("multi_source", {}).get("sample_rate", 48000)),
    )
    input_path = Path(built["input_video"])
    req.input_video = str(input_path)
    source_manifest_path = Path(built["source_manifest"])
else:
    input_path = source_items[0].path if source_items else _prepare_run_input_video(req)
    source_manifest_path = None
    fingerprint = _run_fingerprint(req, input_path) if input_path else ""
    input_name_for_task = _display_input_name(req)
    if input_name_for_task and not req.task_id:
        task_id = _make_task_id_from_fingerprint(input_name_for_task, req.production_mode, fingerprint)
    else:
        task_id = _resolve_run_task_id(req)
    task_dir = ensure_dir(OUTPUTS_DIR / task_id)
```

注意：后续 `_seed_reusable_outputs()`、job 创建、队列逻辑不变。

### 4.3.6 修改 `_build_run_command()`

给函数新增参数：

```python
def _build_run_command(req: RunRequest, task_id: str, input_path: Path | None = None, source_manifest_path: Path | None = None) -> list[str]:
```

在 `if input_path:` 后加入：

```python
if source_manifest_path:
    cmd.extend(["--source-manifest", str(source_manifest_path)])
```

调用处改成：

```python
cmd = _build_run_command(req, task_id, input_path, source_manifest_path)
```

### 4.3.7 新增多源 fingerprint

```python
def _multi_source_fingerprint(req: RunRequest, sources: list[MultiSourceItem]) -> str:
    payload = {
        "sources": [
            {
                "source_id": item.source_id,
                "source_type": item.source_type,
                "path": str(item.path.resolve()),
                "size": item.path.stat().st_size,
                "mtime": round(item.path.stat().st_mtime, 3),
                "remote": _remote_video_fingerprint(item.remote),
            }
            for item in sources
        ],
        "production_mode": req.production_mode,
        "aspect_ratio": req.aspect_ratio,
    }
    raw = json.dumps(payload, ensure_ascii=False, sort_keys=True)
    return hashlib.sha256(raw.encode("utf-8")).hexdigest()[:16]


def _display_multi_source_name(sources: list[MultiSourceItem]) -> str:
    first = sources[0].display_name or sources[0].path.stem
    return f"multi_{len(sources)}_{first}"
```

---

## 5. 修改 `newsclip_agent/pipeline.py`

### 5.1 RunOptions 增加字段

```python
source_manifest: str | None = None
```

### 5.2 parse_args 增加 CLI 参数

```python
parser.add_argument("--source-manifest", help="多源任务映射 JSON，由 Web 多视频输入生成")
```

### 5.3 `_record_run_options()` 增加记录

```python
"source_manifest": self.options.source_manifest,
```

### 5.4 `_init_task()` 写入多源信息

在新任务初始化时，当前代码会写 `task.json` 和 `manifest.json`。在 `task = {...}` 之前读取 source manifest：

```python
source_manifest = read_json(Path(self.options.source_manifest), {}) if self.options.source_manifest else {}
```

然后 `task` 增加：

```python
"source_mode": source_manifest.get("source_mode", "single_source"),
"source_manifest": relpath(Path(self.options.source_manifest), self.task_dir) if self.options.source_manifest else "",
"source_videos": source_manifest.get("sources", []),
```

`manifest` 增加：

```python
"source_mode": source_manifest.get("source_mode", "single_source"),
"source_manifest": task.get("source_manifest", ""),
"source_videos": source_manifest.get("sources", []),
```

如果 `source_manifest` 存在，建议复制一份到任务 input 目录，保证后续任务迁移时自包含：

```python
if self.options.source_manifest:
    sm_path = Path(self.options.source_manifest).resolve()
    sm_dst = self.task_dir / "input" / "source_manifest.json"
    if sm_path != sm_dst.resolve():
        copy_or_link(sm_path, sm_dst)
    task["source_manifest"] = relpath(sm_dst, self.task_dir)
    self.manifest["source_manifest"] = relpath(sm_dst, self.task_dir)
```

注意：由于 Web 已经把 `source_multi.mp4` 放在 `task_dir/input`，`_init_task()` 复制/链接到 `input/source.mp4` 虽然有点重复，但不影响运行。后续可优化为如果输入已经在当前任务目录内，则直接引用原路径。

---

## 6. 前端改造

## 6.1 `web_static/index.html`

### 6.1.1 支持多文件上传

把：

```html
<input id="videoUploadInput" class="sr-only" type="file" accept="video/mp4,video/quicktime,video/x-matroska,video/x-m4v,video/x-msvideo" />
```

改为：

```html
<input id="videoUploadInput" class="sr-only" type="file" multiple accept="video/mp4,video/quicktime,video/x-matroska,video/x-m4v,video/x-msvideo" />
```

### 6.1.2 增加素材篮区域

在 `任务参数` 面板上方或下方增加：

```html
<section class="panel source-basket-panel">
  <div class="panel-header">
    <div>
      <h3>本次剪辑素材</h3>
      <p>支持本地素材、远程素材混合拖拽；按列表顺序拼接分析。</p>
    </div>
    <button id="clearSourceBasket" class="button secondary" type="button">清空</button>
  </div>
  <div id="sourceDropZone" class="source-drop-zone">
    <strong>把多个视频拖到这里</strong>
    <span>也可以从左侧本地素材 / 远程素材点击“加入”</span>
  </div>
  <div id="sourceBasket" class="source-basket"></div>
</section>
```

---

## 6.2 `web_static/app.js`

### 6.2.1 state 增加 sourceBasket

```javascript
sourceBasket: [],
```

每个 item 结构：

```javascript
{
  id: crypto.randomUUID(),
  source_type: "local",
  path: video.path,
  display_name: video.name,
  meta: video
}
```

远程：

```javascript
{
  id: crypto.randomUUID(),
  source_type: "remote_ucms",
  remote_video: video,
  display_name: video.display_name || video.name || "远程素材"
}
```

### 6.2.2 多文件上传

替换 `uploadSelectedVideo()` 中单文件逻辑：

```javascript
const files = Array.from(input.files || []);
if (!files.length) return;
const form = new FormData();
files.forEach((file) => form.append("files", file));
const endpoint = files.length > 1 ? "/api/videos/upload-multiple" : "/api/videos/upload";
```

处理响应：

```javascript
const uploaded = Array.isArray(data) ? data : [data];
uploaded.forEach((video) => addLocalSourceToBasket(video));
if (uploaded.length === 1) {
  state.selectedVideo = uploaded[0];
  previewLocalVideo(uploaded[0]);
}
renderSourceBasket();
```

### 6.2.3 本地素材点击行为调整

当前点击本地素材会直接设为 `state.selectedVideo`。建议保留“预览”，同时增加一个“小按钮：加入素材篮”。如果不想大改 UI，点击本地素材就加入篮子，并预览该视频。

```javascript
function addLocalSourceToBasket(video) {
  if (state.sourceBasket.some((item) => item.source_type === "local" && item.path === video.path)) return;
  state.sourceBasket.push({
    id: crypto.randomUUID(),
    source_type: "local",
    path: video.path,
    display_name: video.name,
    meta: video,
  });
  state.selectedVideo = video;
  state.selectedRemoteVideo = null;
  renderSourceBasket();
  updateActiveTitleForSources();
}
```

### 6.2.4 远程素材点击行为调整

```javascript
function addRemoteSourceToBasket(video) {
  const key = video.remote_id || String(video.id || video.name || "");
  if (state.sourceBasket.some((item) => item.source_type === "remote_ucms" && (item.remote_video.remote_id || item.remote_video.id) === key)) return;
  state.sourceBasket.push({
    id: crypto.randomUUID(),
    source_type: "remote_ucms",
    remote_video: video,
    display_name: video.display_name || video.name || "远程素材",
  });
  state.selectedRemoteVideo = video;
  state.selectedVideo = null;
  renderSourceBasket();
  updateActiveTitleForSources();
}
```

### 6.2.5 渲染素材篮

```javascript
function renderSourceBasket() {
  const box = $("sourceBasket");
  if (!box) return;
  if (!state.sourceBasket.length) {
    box.innerHTML = `<div class="source-empty">还没有选择素材。请从左侧加入或拖拽上传多个视频。</div>`;
    return;
  }
  box.innerHTML = state.sourceBasket.map((item, index) => `
    <div class="source-basket-item" draggable="true" data-id="${escapeAttr(item.id)}">
      <span class="source-index">${index + 1}</span>
      <div class="source-main">
        <strong>${escapeHtml(item.display_name || "未命名素材")}</strong>
        <small>${item.source_type === "remote_ucms" ? "远程素材" : "本地素材"}</small>
      </div>
      <button class="mini-button" data-action="up" data-id="${escapeAttr(item.id)}">上移</button>
      <button class="mini-button" data-action="down" data-id="${escapeAttr(item.id)}">下移</button>
      <button class="mini-button danger" data-action="remove" data-id="${escapeAttr(item.id)}">移除</button>
    </div>
  `).join("");

  box.querySelectorAll("button[data-action]").forEach((button) => {
    button.addEventListener("click", () => updateSourceBasketItem(button.dataset.id, button.dataset.action));
  });
}

function updateSourceBasketItem(id, action) {
  const idx = state.sourceBasket.findIndex((item) => item.id === id);
  if (idx < 0) return;
  if (action === "remove") state.sourceBasket.splice(idx, 1);
  if (action === "up" && idx > 0) [state.sourceBasket[idx - 1], state.sourceBasket[idx]] = [state.sourceBasket[idx], state.sourceBasket[idx - 1]];
  if (action === "down" && idx < state.sourceBasket.length - 1) [state.sourceBasket[idx + 1], state.sourceBasket[idx]] = [state.sourceBasket[idx], state.sourceBasket[idx + 1]];
  renderSourceBasket();
  updateActiveTitleForSources();
}
```

### 6.2.6 拖拽上传

```javascript
function bindSourceDropZone() {
  const zone = $("sourceDropZone");
  if (!zone) return;
  ["dragenter", "dragover"].forEach((name) => {
    zone.addEventListener(name, (event) => {
      event.preventDefault();
      zone.classList.add("drag-over");
    });
  });
  ["dragleave", "drop"].forEach((name) => {
    zone.addEventListener(name, (event) => {
      event.preventDefault();
      zone.classList.remove("drag-over");
    });
  });
  zone.addEventListener("drop", async (event) => {
    const files = Array.from(event.dataTransfer?.files || []).filter((file) => file.type.startsWith("video/") || /\.(mp4|mov|mkv|m4v|avi)$/i.test(file.name));
    if (!files.length) return;
    await uploadFilesToBasket(files);
  });
}
```

`bindEvents()` 里调用：

```javascript
bindSourceDropZone();
$("clearSourceBasket")?.addEventListener("click", () => {
  state.sourceBasket = [];
  renderSourceBasket();
  updateActiveTitleForSources();
});
```

### 6.2.7 修改 `runSelected()`

在 `render_only` 之外优先判断 `sourceBasket`：

```javascript
} else if (state.sourceBasket.length) {
  req.source_items = state.sourceBasket.map((item) => {
    if (item.source_type === "remote_ucms") {
      return {
        source_type: "remote_ucms",
        source_id: item.id,
        display_name: item.display_name,
        remote_video: item.remote_video,
      };
    }
    return {
      source_type: "local",
      source_id: item.id,
      display_name: item.display_name,
      path: item.path,
    };
  });
  req.input_video = null;
  req.remote_video = null;
  req.task_id = taskId || null;
```

注意：这段要放在 `state.selectedVideo` 和 `state.selectedRemoteVideo` 判断之前。否则单击左侧素材后会走单视频老逻辑，而不是素材篮多源逻辑。

---

## 6.3 `web_static/styles.css`

新增样式：

```css
.source-basket-panel {
  margin-bottom: 16px;
}

.source-drop-zone {
  border: 1px dashed var(--line);
  border-radius: 16px;
  padding: 18px;
  background: rgba(255, 255, 255, 0.04);
  display: flex;
  flex-direction: column;
  gap: 6px;
  align-items: center;
  justify-content: center;
  min-height: 88px;
  color: var(--muted);
}

.source-drop-zone strong {
  color: var(--text);
}

.source-drop-zone.drag-over {
  border-color: var(--brand);
  background: rgba(255, 102, 0, 0.08);
}

.source-basket {
  margin-top: 12px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.source-basket-item {
  display: grid;
  grid-template-columns: 32px 1fr auto auto auto;
  gap: 8px;
  align-items: center;
  padding: 10px;
  border: 1px solid var(--line);
  border-radius: 12px;
  background: var(--panel-soft);
}

.source-index {
  width: 26px;
  height: 26px;
  border-radius: 999px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: var(--brand);
  color: white;
  font-weight: 700;
}

.source-main {
  min-width: 0;
}

.source-main strong,
.source-main small {
  display: block;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.source-empty {
  padding: 12px;
  color: var(--muted);
  border: 1px dashed var(--line);
  border-radius: 12px;
}
```

---

## 7. config.toml 建议新增

```toml
[multi_source]
fps = 25
sample_rate = 48000
normalize_before_concat = true
keep_source_manifest = true
```

---

## 8. 兼容性要求

1. `python run_web.py` 不改。
2. 原单视频上传、单视频运行必须继续可用。
3. 原远程单视频运行必须继续可用。
4. 多视频任务前端统一走 `source_items`。
5. 多视频任务后端最终仍调用 `run_pipeline.py --input <source_multi.mp4>`。
6. AI 配音解说模式、高光重组模式都要支持多源。
7. 多源任务 `manifest.json` 必须能看到 `source_mode`、`source_videos`。
8. 删除任务时会删除 `source_multi.mp4` 和 normalized_sources，避免堆积。

---

## 9. 测试用例

### 9.1 单本地视频回归

- 上传 1 个本地 mp4。
- 运行 AI 配音解说。
- 确认命令仍只有一个 `--input videos/...mp4`。
- 确认输出正常。

### 9.2 多本地视频

- 拖拽 3 个本地 mp4 到素材篮。
- 调整顺序。
- 运行高光重组。
- 检查：
  - `outputs/<task>/input/source_multi.mp4` 存在。
  - `outputs/<task>/input/source_manifest.json` 存在。
  - `manifest.json.source_mode == multi_source_concat_proxy`。
  - `manifest.json.source_videos.length == 3`。
  - 生成视频正常。

### 9.3 本地 + 远程混合

- 左侧加入 1 个本地素材。
- 远程素材加入 1 个。
- 运行 AI 配音解说。
- 检查远程视频被下载，拼接代理视频正常。

### 9.4 远程多素材

- 远程列表加入 3 个素材。
- 运行高光重组。
- 检查下载缓存与拼接结果。

### 9.5 顺序校验

- 选择 A、B、C 三个 10 秒视频。
- source_manifest 中：
  - A virtual_start = 0
  - B virtual_start ≈ 10
  - C virtual_start ≈ 20
- 最终成片所用时间轴应来自拼接后的全局时间轴。

---

## 10. V2 原生多源渲染方向

V1 使用 `source_multi.mp4`，所有 clip 时间都是虚拟拼接时间轴。V2 可进一步优化为原生多源：

1. timeline chunk 增加 `source_id`。
2. ASR segment 增加 `source_id` 和 `local_start/local_end`。
3. highlight candidate 增加 `source_id`。
4. cut_plan/reassembly_cut_plan clip 增加 `source_id`。
5. render 时按 `source_id` 找原始素材裁切。

但 V2 改动会涉及全链路，不建议和 V1 一次完成。
